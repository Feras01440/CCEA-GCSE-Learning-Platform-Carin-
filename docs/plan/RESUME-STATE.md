# Resume state — 13 September 2026

Two Claude sessions work in this repo. This (UX) session owns the app, UX, verification and QA; the sibling session (ccea-gcse-top-learning-e3) owns content production (packs/*/content/**, data/decks/**), the media layer (src/components/media/*, data/links/**, StepRevealNote block kinds photo/video/sim, the SeeIt panel) and the authoring briefs (pipeline/prompts/*).

## Done today
- Content: 6 H bundles published and human-read (M4 histograms, algebraic fractions, circle theorems, quadratics/show-that, bounds; M3 reverse percentages). Decks: science P1 (208) and P2 (190), maths M3 (182) and M4 (92) authored — 850 cards shipped; baseline decks generated for B1/B2/C1/C2 from the taxonomy recall data.
- App: flashcards (/flashcards, /flashcards/[subject]/[unit], topic preselect via ?topic=), mixed practice (/practise), review inbox, topic page anatomy, cairn motif (Today tile, Map stacks, close cards), journey chart, progress lines, haptics, skeletons, export/import backup, service worker (scripts/build-sw.mjs), vercel.json, docs/dev/deploy.md, e2e suite (agent running).
- Emotional design rules: docs/plan/emotional-design.md (no confetti/XP/streak pressure; five-question audit).
- Bulk authoring runs on model "opus" (Fable limit killed agents on 6 Sep).
- G7 human reads (13 Sep): sibling's maths.m8.surds and maths.m8.equation-of-a-circle-and-its-tangent read line by line and passed; one content fix sent back (surds insight rule said "rationalise with the conjugate", contradicting the note and the corpus). Digest helper for reads lives in the session scratchpad (digest-bundle.mjs: prints WE/dx/q/ftm/rp/insight in plain text).
- FM1–FM3 decks (548 cards) read card by card: all arithmetic, formulae and spec-exclusion claims check out against data/spec/further-mathematics.json; one muddled FM3 binomial rounding card sent back (both fixes verified on disk 13 Sep). Full chain verified 13 Sep: npm run build green (21 bundles, 12 decks, fix-export 817 payloads, sw 2547 URLs), npm test 630/630, npm run e2e 50/50 (two new offline soft-navigation tests).
- G7 read of maths.m8.sine-rule-cosine-rule-and-area-of-a-triangle: all answers verified by recomputation; four intermediate-value slips in printed solutions and two commonError values sent back; asked for ExaminerSource ids normalised to M82. Sibling reported the fixes applied at ~04:37 but the bundle on disk was unchanged (mtime 04:33:32, all 78 sources still M8); told them; re-verify with the scratchpad script verify-trig-fixes.mjs when they re-apply.
- G7 read of the M4 batch (factorising ax²+bx+c, perpendicular lines, frustums/compound solids, stratified sampling): all values verified; five fixes sent back (two floating-point artefacts in sampling text, two commonError values that do not arise from the error described, one optional). All five verified on disk 13 Sep (mtime 04:46:13); float-formatting rule added to the sibling's batch brief. Trig-bundle fixes re-applied by the sibling and verified on disk (mtime 04:42:35, 78 sources M82).
- Checked panel: verification-log detail strings are machine traces written by the authoring agents (x**2, 1e-9, 15-digit floats; 49 of them across four bundles). No data rewrite: CheckedPanel now sets detail as a monospaced log line so it reads as a trace, not prose.
- SVG rule: the 33 authored figures in my circle-theorems and quadratic-formula bundles carried a colour-only <style> block (svg{color} + prefers-color-scheme); stripped from the source bundles with the scratchpad script strip-svg-style.mjs (guarded: only colour-only blocks are removed), content rebuilt, repo-wide rescan clean (0 style blocks in packs or public/content).
- Platform (13 Sep): algebraic AnswerSpec gained optional `form` (factorised | expanded | simplest-fraction | completed-square | y=mx+c | surd-rationalised | single-fraction | integer-coefficients | subject); "subject" = spec's LHS symbol alone on one side, absent from the other, wired through toAlgebraSpec to the engine's form mode (checkSubject in src/lib/marking/algebra.ts). `order` answer kind now auto-marked (src/components/items/order-marking.ts, raw = indices top-first "2,0,1") with a drag-or-arrows OrderField in AnswerField (seeded shuffle so the authored order never shows); added to maths/further-maths pack.json answerKinds; JSON schemas regenerated. Known engine gap left open: equations are compared only up to a constant factor, so x(8−w)=12+3w vs x=(12+3w)/(8−w) reads as not-equivalent rather than wrong-form.
- G7 (13 Sep): maths.m7.standard-form, maths.m7.changing-the-subject-harder-formulae and maths.m3.difference-of-two-squares pass with nothing to fix.
- G7 (13 Sep, later): maths.m7.index-laws pass; maths.m3 difference-of-two-squares and simplifying-multiplying-dividing pass; maths.m3 algebraic-fractions-with-numerical-denominators has 7 commonError values that do not follow from their described errors (sent; M3 author re-deriving every numeric commonError in all nine M3 bundles by script); maths.m3 solving-quadratics has 12 two-root answers encoded as a single numeric (engine supports solution sets: encode as algebraic latex "x=3, x=7") plus 2 bad commonErrors; maths.m3 straight-lines has 3 coordinate answers encoded as a single numeric and an "any parallel line" part that cannot be auto-marked as one fixed line (sent). Sibling edited md.ts/Markdown.tsx/Figure.tsx (bold-over-maths renderer fix) — accepted, 646 tests + tsc still green; asked for a heads-up before future edits under src/components/items or src/lib.
- Pair-aware marking (13 Sep): src/lib/marking/points.ts — an algebraic spec whose latex is coordinate tuples ("(2, 3)" or "(1, 2), (-3, -6)") is marked as a set of pairs (crossed pairing rejected with feedback, missing/extra pairs named); learner may type tuples or x = …, y = … assignments; wired in mark.ts before the algebra engine. Convention sent to the sibling for simultaneous equations and graph intersections.
- G7 S batch (13 Sep): hcf-lcm, bounds add/mult, identities, factorising x²+bx+c — maths correct; spec/text fixes sent (minutes-and-seconds answer, two-answers-as-one-numeric parts, a garbled hint, two commonError values).
- Backlog (mine): equation equivalence beyond a variable clearing factor is done; remaining self-marked plots are region, box and best-fit (4 + 0 + 0 parts today).
- Marker: text parts now share the part's tariff across key-word groups (floor of the fraction earned; marksAvailable always = part.marks), so 1-mark "state, with a reason" parts written as two groups need both for the mark. Tests in src/components/items/mark.test.ts.
- Static export fix: Next 16 exported RSC prefetch payloads as nested folders (out/learn/maths/M4/__next.learn/$d$subject/$d$unit/__PAGE__.txt) while the client requests the dotted file beside the route, so every prefetch 404'd. scripts/fix-export.mjs writes the flattened copies (813 files, idempotent) and runs in `npm run build` before build-sw; the service worker now precaches the `__next._tree.txt` and flattened `__PAGE__.txt` payloads so offline taps stay soft navigations. Verified in the browser: every __next request on /learn/maths/ → M4 returns 200.
- G7 B1 batch (13 Sep): eight Biology B1 bundles read (leaf structure, enzyme factors, cells and microscopy, photosynthesis equation/limiting factors, photosynthesis investigations, food tests, biological molecules/food energy, enzymes and digestion). Every number recomputed and correct (magnifications, ratios, rates 13.2÷30, percentage changes, energy-content chains 20×4.2×19=1596 → 3990 J/g, best-fit anomaly at 4 h). Sent: two greedy commonError regexes in food-tests (`X.*starch` crosses into later samples), the food-mass commonError value in biological-molecules q0004 (134.4 not 67.2 when the student carries on), and the presence-based limit on "assign A/B/C" text parts (a swapped assignment scores full marks; split into sub-parts). M3 author's correction pass spot-checked on fresh digests (hcf, bounds, identities, algebraic fractions, solving quadratics, straight lines): every value verified, nothing sent back. Transformations q0013 verified on disk. maths.m7.similar-shapes read line by line: pass.
- Marker (13 Sep, from the B1 read): key-word matching drops apostrophes (Benedict's = benedicts) and lets a single word of 4+ letters match its inflections from a closed list of endings (denatur → denatured/denaturation, heat → heated, increase → increasing, mitochondri → mitochondria) but never "cell" → "cellulose" or "ratio" → "rational"; before this, authored stems such as "denatur"/"diffus"/"respir" matched nothing. A numeric answer with the zeros dropped ("150" for a 2-d.p. spec) stays correct and the feedback adds the write-150.00 reminder. `order` parts test text commonError patterns against the arranged item texts, and duplicate item texts are interchangeable. New lint src/components/items/keyword-lint.ts runs in the content build: a key-word group no accepted answer can earn is printed as KEYWORDS (non-fatal) and counted in the summary (4 warnings + 10 soft at 46 bundles; sent, all fixed by the sibling the same hour; the sibling added scripts/keyword-soft.mts to list the soft ones). 664 tests, tsc and content:check green.
- Marker (13 Sep, later): a d.p./s.f. tolerance is a closeness test only; it governs the written form (too many places wrong, dropped zeros right with the write-150.00 reminder) only when the part's stem instructs the accuracy (`instructsAccuracy` in mark.ts, instruction sentence with decimal places / significant figures / d.p. / s.f. / nearest; QuestionRunner passes part.stem, the twin passes its stem). Reason: 58 numeric specs use tol dp as a tolerance on values such as 1.8 or 760. normaliseText spaces relation signs ("x=6" → "x = 6") and expands ± ("±6" → "6 -6"), so "values of x" key words match however the pair is typed. Sent the sibling the solution-set encoding for inverse-proportion q0006/q0014(b)/q0015/WE03 twin. My quadratic-formula bundle: q0003 re-encoded as the solution set x=1/2, x=-4/3; q0016(b) reduced to two key-word groups.
- G7 (13 Sep): maths.m8.inverse-proportion read line by line, every number recomputed: pass.
- G7 (13 Sep, afternoon): M8 fractional-and-negative-indices, index-laws-in-algebra, recurring-decimals, growth-decay; M8 simultaneous (linear + non-linear), graphical-solution-of-quadratics, gradient-of-a-curve; B1 part 2 (respiratory surfaces, aerobic, anaerobic, nervous system, reflex arc, blood glucose, kidney): every value recomputed; eight small fixes sent (a commonError equal to the right answer, a duplicate mcq option, two float-artefact spec values, three bare-"A" key words that match the article, a matching task encoded as `order`).
- G7 (13 Sep, later): M7 batch A (surds-and-pi, index-laws-integer, simultaneous-algebraically, inequalities-regions) recomputed; sent one defect (regions q0007 marks (11, 11) as inside a region it is outside) and two commonError-route nits.
- G7 (13 Sep, later): M7 batch B (nth-term, graph recognition, quadratic graphs/intersections, direct proportion): maths correct; sent four "${…}" generator leaks in quadratic-graphs and one in the direct-proportion note, a duplicate mcq option, a FTM built on an exact k (27/36 = 0.75, so its "rounding error" is not one), a 6/x vs 9/x mismatch and a missing "$". Guard added: pipeline/build-content.mts fails content:check on any "${" in a bundle or note file, and src/components/items/content-lint.ts (wired there too) fails it on duplicate option texts, a common error the spec would mark correct, and float-artefact values; the first run found 8 across 66 bundles (my circle-theorems q0018(a) fixed; the rest sent).
- Marker (13 Sep, later): a unit phrase typed after a number ("36 breaths per minute", "9 mmol/dm³", "-5.5 °C per minute") is read as the unit instead of making the answer unreadable; unknown units compare loosely ("breaths/min" = "breaths per minute"). e2e: the full run showed 8 one-minute timeouts (Desktop offline ×2, Pixel 7 ×6) while vitest, tsc and content:check ran alongside; the same specs pass 26/26 alone, so run the suite on a quiet machine.
- Marker (13 Sep, afternoon): LaTeX in a pair spec (`\sqrt{}`, `\frac{}{}`, `\pi`, `\left(`) is read as plain spellings (src/lib/marking/points.ts texToPlain), so surd-valued pairs accept tuple and assignment spellings; currency/percent signs dropped before key-word matching; a key word earns one group only (two "give two symptoms" groups with the same list need two different symptoms). e2e learn spec accepts the "Lesson and practice" badge.
- Transformation grid (13 Sep): `graph` answers with plot "transformation" are now auto-marked and interactive. Engine src/lib/marking/transformation.ts: the placed vertices are compared with the image as a set; a wrong shape is identified against a library of single transformations (reflections in the axes, y = ±x and any x = k / y = k; rotations of 90°/180° about any half-lattice centre; translations; enlargements by 2, 3, 4, ½, ⅓, −1, −2, −½ about lattice centres) so the feedback says what she drew against what was asked; a graph commonError whose `test` lists the wrong image's vertices ("image drawn at (-3, 1), (-8, 1), (-3, 4)…") matches that set. UI src/components/items/TransformationField.tsx: squared grid (at least −8…8), the object drawn, the mirror line or centre parsed from the stem (src/components/items/grid-guides.ts), tap a grid point to place a vertex / tap again to lift it, undo and clear, a typed coordinate fallback, Check enabled only when every vertex is placed; QuestionRunner passes the part stem. Verified in the dev preview on the M7 transformations topic: correct image → "Every vertex of the image is in the right place" 2/2; first-stage-only drawing on the combined item → "That is a reflection in the line y = x of the object, which is not the image asked for here." Other plot kinds (histogram, points-line, curve) still self-mark.
- G7 (13 Sep, 13:10): the sibling's per-stage split of the M7 combined-transformations bundle (q0008/q0014/q0015 now (a)/(b) draw parts with expect.object/expect.image per stage; q0018's object changed to (2, 1), (6, 1), (6, 3) after describeMapping showed the old one had two correct single-transformation answers) verified with the scratchpad probe probe-transformations.mts: all 11 graph specs mark their image correct, describeMapping names each exactly as the stem asks, all 14 graph commonError tests parse to a vertex set and fire on it. Batch-B fixes verified on disk (paired dollars, y = 9/x, 54 / 6 / 54.27, FTM03 now a real error, distinct dx01 options, tidy line latex); batch-A fixes (regions q0007 classification via satisfies(), simultaneous q0007/q0010 ERR routes) applied per the sibling. content:check: 68 published, 0 key-word warnings; the one problem is the in-progress B1 fieldwork-sampling bundle (dp tolerance without places, told the sibling). Export rebuilt after these engine changes (docs/dev/build-latest.log); e2e rerun alone is the next check.
- Algebra engine (13 Sep, 13:30): equations that differ by a variable factor are recognised when one side is a formula for a symbol (x(8 − w) = 12 + 3w against x = (12 + 3w)/(8 − w)): `sampleSolves` in src/lib/marking/algebra.ts checks the equation is linear in the symbol and satisfied by the formula, so form "subject" now says "right relationship, x is not the subject yet" instead of "not equivalent"; a sign slip in the unrearranged equation still reads as a sign error; x² = wx/4 stays wrong. Graph marking: when a drawn transformation matches a graph common error the engine's identification leads and the authored diagnosis follows (mark.ts withCommonError `lead`). Tests 97/97 algebra, all green; documented in docs/dev/README.md.
- G7 (13 Sep, 13:40): M3 geometry batch (circle-parts, cylinder-cone-sphere, pressure, sohcahtoa) and M8 batch C (negative enlargements, similar 3-D, conditional probability) read line by line, every value recomputed; maths correct throughout; seven value fixes (M3) and four fixes plus two nits (M8) sent and verified applied by the sibling. A corpus-wide dollar scan then found the FM1 expand-three-brackets solutions missing their opening "$" and one in M4 frustums; both fixed. content-lint now fails on an unpaired "$" in any learner-facing string and on prose words inside a maths segment (tests added); content:check 73/0.
- Offline copy (13 Sep, 14:00): the service worker precached the whole export at install, 2,601 URLs and 60 MB (821 pages, 1,638 RSC payloads, 70 content files), which explained the e2e collapse under load (8 contexts each installing 60 MB through one `serve`) and would have downloaded 60 MB silently on her phone's first visit. scripts/build-sw.mjs now precaches the shell only (about 10 MB) and embeds the on-demand set (topic pages and payloads, content JSON, paper pages, about 50 MB); the worker answers `offline-copy:status` and runs `offline-copy:start` with progress messages; src/lib/offline-copy.ts is the page side; Settings gained "Study without signal" with "Download the full copy" (progress line and bar, then "Every topic and paper is ready offline."), remembered per device and re-requested by ServiceWorker.tsx after each new version. e2e offline spec: the visited-page test opens the topic online first; a new test downloads the full copy from Settings and opens an unvisited M8 topic offline; the flashcards spec waits for the first checkbox before counting.
- Plot field (13 Sep, 14:30): the 21 graph parts with plot points-line (cumulative frequency curves, a straight-line graph, science results with a best-fit line), curve (quadratic, reciprocal, exponential samples) and histogram (M4) stop self-marking. src/lib/marking/plot.ts marks table points within tolerance (a stray point in a missing point's column is named "belongs at"), a two-tap line against its two lineThrough points, and bar heights within scaleTolerance; src/components/items/PlotField.tsx is the grid (data-scaled axes, small-square snapping, join drawn once complete, typed fallbacks); wired through AnswerField, spec-map (isAutoMarkable, expectedDisplay) and mark.ts (marks shared in proportion). 711 unit tests green; export rebuilt (212 shell URLs, 2,397 on demand); e2e/plot.spec.ts 6/6 on Desktop and Pixel 7 (wrong heights named, published heights correct, typed points plus a two-tap line). Box plots (plot "box") followed at 15:00 at the sibling's request for the M3 handling-data batch: BoxPlotField (scale, five values in order, live box and whiskers) and checkBox (value by value within tolerance); no published part uses it yet, so the e2e case waits for the first one. Regions (plot "region", the 3 M7 inequalities parts) followed at 15:20: RegionField draws the boundaries and shades the side of each that one tap fixes; region.ts parses linear inequalities and marks each one the point satisfies. Only best-fit plots self-mark now; export rebuilt and e2e/plot.spec.ts is 8/8 on Desktop and Pixel 7 (region case reads the first region part from the published JSON, finds a point inside it and taps it). B1 part 3 (six bundles) arrived at 15:15 and is being pre-read by three background agents (docs/dev/qa/pre-read/b1-part3-{a,b,c}.md) before my own verification. Two background agents launched at 14:45: a render crawl of every topic page in the export (scripts/qa/crawl-export.mjs → docs/dev/qa/crawl-export.md) and a UX/accessibility audit of the two grid fields (docs/dev/qa/grid-fields-audit.md).

- STOP POINT (13 Sep, 14:10 local; clock note: the "13:30–15:40" stamps in the entries above are about 90 minutes fast, the work ran 13:10–14:10). The user's standing rule from 14:00: both sessions stop when the 5-hour usage window reaches 80%; the sibling estimated that at 14:15 and stops its authors then. State at the stop: tsc clean; 722 unit tests green plus the content-lint SVG-NaN test; content:check was red at 14:08 only on the sibling's m3 box-plots bundle (authored SVG figures with NaN in the box path and rect; its author was fixing the q1/q3 scale before 14:15; my new lint in content-lint.ts fails any svg figure containing "NaN" or "undefined"); figure captions (generated figures, photos) and the formula-sheet panel now render maths through Tex/MdInlines (crawl found raw "$" there); export NOT yet rebuilt after those two renderer fixes and the box/region fields are in the export from 12:44 only partly (box plot: no published part yet; regions: e2e 2/2). Background agents at the stop: UX/accessibility audit of the grid fields (report expected at docs/dev/qa/grid-fields-audit.md) and three B1 part-3 pre-reads (reports expected at docs/dev/qa/pre-read/b1-part3-{a,b,c}.md; digests already there); their findings are NOT yet verified by me and must be checked against the bundles before anything goes to the sibling.
- 14 Sep 03:00 RESUMED on the user's word ("I gave a task to (fork), help him out"): the sibling's ordered list is my plan (groups 1–5 in its 03:05 message). DONE 03:20: QuestionRunner paper clause deleted (316 text parts, 792 marks, now key-word marked) with a "My answer matches the solution: award N/N" override that records a self-awarded attempt and flips the card (verified in the preview on b1-enzyme-factors q0001: 1/2 → award → 2/2); onDone now fires on advance so the override counts; DiagnosticWithConfidence no longer prints the skill id and says "Check". AGENTS RUNNING: grid-field audit must-fixes (PlotField/TransformationField/ui/plot.ts/content-lint), e2e helper update + full build + e2e run (e2e/, scripts/qa), CheckSection extraction + recheck + flow persistence (TopicContent minimal edit, db.ts version bump, src/lib/session/flow.ts), appearance A (globals.css type-scale and surface tokens, data-subject tints, Nav More menu, theme default system), three B1 pre-reads (note reading deferred until the sibling's v2 note pass lands). ALSO DONE 03:40: normaliseText drops the degree sign ("96°" earns "96"); the numeric marker treats a percent sign as the unit when the spec unit is % ("45%" passes acceptForms decimal; 310 numeric tests green); scripts/qa/text-parts-vs-solutions.mts + docs/dev/qa/text-parts-vs-worked-solutions.md (29 zero, 74 partial of 322 text parts) handed to the sibling for a key-word fixer; B1 part-3 pre-read A (plant hormones, fieldwork) verified in sample and sent (docs/dev/qa/pre-read/b1-part3-a.md); the practice-flow survey is in the transcript of agent a604 (sets: q./dx./rp./ftm. ids, no renderer; ensureCard only for prompt/diagnostic; no twin on questions, only WorkedExample.twin; ProgressLine idiom in MixedPractice; ReviewComplete is the close-card template; QuestionRunner fires onDone on advance). NEXT after they land: review each diff, rebuild, full e2e, then group 2 (hero, eyebrows as verbs with minutes, three surfaces at the 56 call sites), group 3 (ensureCard every kind, onTwin, one-question-at-a-time practice with ProgressLine, bundle.sets, close card), group 4 (Table/Steps/Label fields, QWC surface, fixMatches, WorkingLadder), group 5 (first run into a seeded lesson, timing data, UMS progress view, run-up-to-papers on Today).
- 14 Sep ~03:55: the 4am session limit killed all seven agents mid-task. 19 Sep 01:30 (the user restarted the sibling on Sat 01:28; my continue was the earlier one): on disk from them, tsc clean and 749 unit tests green: grid-field fixes (plotLattice-derived small squares, typed line input, contrast, wording without "wrong", lattice lint), CheckSection + "Check again" recheck + src/lib/session/flow.ts (Dexie v3 `flow` table, selectCheckItems, answeredGateIds) wired in TopicContent, appearance A (type-scale tokens and data-subject tints in app/globals.css and the learn pages, Nav "More" sheet, theme default system, Settings copy), e2e specs updated for the teach-first page (not yet run), table.ts and label.ts markers (fields unfinished). Engine: listing rule now counts only list-shaped answers (segments of at most four words), degree sign dropped in key words, percent sign as the unit. Pre-reads a and b verified and sent; c relaunched. Relaunched 01:35: fields completion, pre-read c, faded-step verdicts. 01:50: preview checks passed (teach-first page, Check yourself, phone More sheet); export rebuilt (102 bundles) and e2e per spec: learn 12/12, offline 8/8, transformation 4/4, plot 0/8 under load (ERR_ABORTED gotos and 60 s timeouts, rerun alone pending), the rest queued; PracticeFlow agent launched (sibling agreed the TopicContent edit); content-lint gained a fatal leaked-value check (NaN/null/[object Object] anywhere, undefined next to a brace, backslash, equals or digit; solutionProgram excluded) and content:check is 102/0; the sibling filed FM2 A (4), B2 A (4), P2 A (4) and the two exemplar v2 notes for G7 (pre-reads to launch as slots free); the companion spec (docs/plan/companion/2026-09-19-companion-spec.md, "Rowan") reviewed and agreed with conditions sent (unsigned support slot, copy-only evening state, no hard-coded facts, upgrade-moment first Letter, memory list with delete and export exclusion, plain mode first fortnight, mock-entered slot, dry lines); the top-tier mock (served copy at out/showcase.html) read and the hero/spine plan aligned to it (Literata self-hosted, word-count minutes until timing data). 02:00: every e2e spec file passes on its own against the rebuilt export (learn 12, offline 8, transformation 4, flashcards 6, today 6, practice 4, papers 4, map-ledger-settings 8, first-run 4, plot 8 when rerun alone: 64 tests), so the check cut-down, appearance A, grid-field fixes and offline copy are verified end to end. User preference relayed by the sibling: execution agents on Opus 5 unless a task genuinely needs Fable (pass model "opus" on launches from now on). Hero contract: the sibling adds a `hero` object to notes (lede ≤ 60 words, three "you can" lines, hero figure id, minutes) starting with the two exemplars; my hero reads it and falls back to the first paragraph. 02:10 LANDED: faded worked-example steps now give a real verdict when a step has no input spec (mistake-marking.ts workingLines/stepLineMatches over fixMatches, with a guard so prose steps and non-result lines never auto-match; WorkedExampleAsQuestion LineStep: "That matches the line I had." / "Not the same line yet. Mine was: …" with "That is what I had" and "Not yet"; 146 items tests green). Hero note block `{type:"hero", lede, can[3], minutes}` added to the NoteBlock union (gates.ts) and skipped by StepRevealNote; both exemplars carry it. 02:25: chemistry strip gained superscript ²/³ and normaliseEquation reads a plain digit before ⁺/⁻ as the charge (Fe3⁺ = Fe^{3+}; NH₄⁺ unchanged; 15 equation tests green); C2 batch A (reactivity series, displacement/extraction, redox, rusting) filed for G7; B1 note pass landed (every note has a hero block, generated ones flagged). 03:05: B1 part-3 pre-read c (nitrogen, eutrophication) verified in sample and sent (docs/dev/qa/pre-read/b1-part3-c.md, N1–N18, M1–M22); all six B1 part-3 bundles are now read. Engine follow-ups from it: text common errors must not raise key-word marks (marksTypicallyEarned floor), fixMatches should treat hyphens as spaces, reject words take inflections (documented for briefs). 19 Sep 23:12 LANDED (agent; suite 1009, tsc 0): /papers as her plan (src/lib/papers/plan-view.ts + 13 tests, PaperPlan.tsx; eleven entries soonest first with days remaining, Foundation hidden for Higher entries, best/latest run as raw → UMS → grade, one 'Show every paper' toggle mounting the old picker; runner close shows raw → UMS → grade; e2e/papers.spec updated: title 'Your papers', tabs asserted after the toggle, 'Run it timed'). Pending: re-teach agent (QuestionRunner) and FM1 batch C pre-read; then rebuild + e2e. 19 Sep 23:08: 'Pause here' stopping point inserted by the app before the heading that starts the second half of any lesson with 4+ sections (lesson-plan withPause, NoteBlock 'pause', StepRevealNote line, data-testid pause-here); learn.spec asserts the hero (minutes, figure, Start the lesson). Suite 1006+ green, tsc clean. Sibling: spokenTitle/shortTitle + topicTitleShort gate on dry.sly, topic.resume on a 1-day cooldown. 19 Sep 23:02: engine: trig solution values typed with ° or "degrees" are the bare value (splitSolutions; FM1 rescue commonErrors can go); '12%' under a d.p./s.f. instruction fixed for real (the earlier 'does not reproduce' was my probe's acceptForms). topic-open verified rendering in the hero on dev (dry.sly with the full long title: asked the sibling for a shortTitle or a length gate). FM1 batch C pre-read agent launched (fm1-c-1.md). Three agents running: re-teach + support slot, papers as a plan, FM1 pre-read. 19 Sep 22:57: fourth build good (out/content complete); e2e on it: learn 12, practice 4, plot 8, transformation 4, offline 8, today 6, first-run 4, flashcards 6, papers 4, map-ledger-settings 8 green; companion 6 of 8 (the 'question on screen' test had a strict-mode locator, fixed with .first()). topic-open wired in TopicHero (firstVisit from mastery, sectionNumber from answered gates, examinerFlagged from the bundle's insight); companion spec's rule is now containment + document order. Engine: '12%' under a d.p. instruction fixed (writtenValue in checkAccuracy), listing rule ignores clause continuations, every phrase word tolerates a trailing s, hyphen between words is a space. Sibling closed B1 part-3 residuals, P2 batch A and B2 batch A. Re-teach and papers agents still running; rebuild + companion/learn rerun after they land. 19 Sep 22:52: sibling's live.ts fix landed (read-only querier, seed in an effect; live.test.ts). My third build had silently failed Next's type check on that test file while it was mid-edit (the export in out/ stayed the 22:47 one, content folder partial from the wipe race); fourth build running with the exit line checked. Two Opus agents running: re-teach on an unrecognised miss + support slot (QuestionRunner, quality-bar item 6) and /papers as a plan (item 8). Map plan tile now a skeleton. Asked the sibling to decide where topic-open lives (topic page always has a question in the DOM). Next: e2e reruns (learn, companion, practice, plot, transformation, offline) on the fresh export, then first run inside a seeded lesson (item 7) once the re-teach agent is out of QuestionRunner. 19 Sep 22:48: hero/spine LANDED (agent; TopicHero, LessonSpine, lesson-plan.ts; Literata self-hosted; hero minutes now computed from the note so hero and rail agree). First full e2e run after the rebuild: today, first-run, flashcards, papers, map-ledger-settings green; learn, companion, practice, plot, transformation, offline red for two root causes, neither in the specs: (1) the export had no maths or science content because build-content wiped public/content while the other session's next build copied it (fixed: the publisher writes in place and deletes stale files after); (2) Rowan is silent everywhere because useCompanionContext seeds companionState inside a liveQuery querier and Dexie throws ReadOnlyError (sibling's live.ts; fix requested). Rebuild + rerun after the live.ts fix. B1 part-3 re-verification filed (2 ready, 4 one line short). Hyphen between words now a space in key-word matching. 19 Sep 22:40 (after the 04:40 limit reset; sibling now at uds:\.pipeLOCALcc-msg-889d5048dcc9e6fec38cbaedad02557f): five agents killed at ~02:40 were resumed by SendMessage; companion wiring LANDED (six slots, e2e/companion.spec.ts; agent found the cooldown re-query bug in live.ts/CompanionLine, held with a ref in Today/CloseCard/PaperRunner, proper fix is the sibling's); C2 and P2 pre-reads in (c2-a-1.md, p2-a-1.md; P2 B1 is a wrong constant, 1500 m/s for air). Engine fixes from C2: chemistry formulae keep brackets (MgNO32 ≠ Mg(NO3)2), $…$ wrapper ignored, instructsAccuracy reads "show your working and give your answer to …". Backfill of review cards for old attempts runs once on database open (src/lib/db/backfill.ts); inbox gate cards carry the nearest preceding figure; tsconfig excludes docs/. Waiting: hero agent, B2 heart/blood pre-read, B1 part-3 re-verification. 02:45 LANDED (agent, verified: tsc 0, vitest 948/948): review cards for every itemKind. record.ts cardIdFor (twins and fades collapse to `${we.id}#twin`), gradeFromMarks (full good / partial hard / none again; null correct = no card), advanceCard (moves FSRS state without a second attempt row), reviewInboxCard carries itemKind; resolve.ts findInBundle resolves prompt, diagnostic, question part, mistake, gate and twin; ReviewInbox renders them with QuestionRunner, FindTheMistake, StepRevealNote (single gate) and WorkedExampleAsQuestion twin. No backfill of existing attempts (separate job if her data matters). Wanted: a `single` prop on StepRevealNote to drop the note-level status lines when one gate is reviewed (after the hero agent lands). Predicted recall now averages over more cards, so Proficient arrives later and more honestly. Also: /learn cards say "N of M topics built"; JourneyChart has a designed empty state (data-testid journey-empty); ExamMap's "Loading your plan…" skeleton waits for the wiring agent. B1 part-3 re-verification agent launched. 02:30 (clock verified; the stamps before this one in this segment were estimated and run up to two hours fast): figure-draw lint fatal in content-lint (svgDrawDefects, lintNoteBlocks, build-content hook); B2 batch A regenerated by the sibling (files keep their old mtime: verify by content); content:check 107/0 with the lint on. The B2 heart/blood pre-read agent told to re-read the regenerated files. 02:20: the sibling's fixer diffs on my five M4 bundles verified with the marker (worked solutions full, wrong reasons at the number's mark); found and fixed an engine gap: algebraic key words now match with operator spacing ignored and ² ³ as ^2 ^3 (phraseIn isAlgebraic/compactOperators; text-parts report still 0/0 of 335). Sibling: B1 part-3 six bundles ready for my re-verification (agent queued behind the pre-reads); FM2 batch A closed; B2 batch A regenerating from the SVG path defect, scripts/qa/svg-draws.mjs to lift into content-lint after it lands. 02:05: B2 pair-1 pre-read forwarded (docs/dev/qa/pre-read/b2-a-1.md; both bundles hold: SVG path data as <g> text so graphs draw nothing, reversed-direction text parts scoring full marks, answers printed in figures); its X1 (8% rejected) does not reproduce and was dropped; its F12 was mine and is fixed (unit phrases carry unicode superscripts, "0.45 mol dm⁻³" reads). Companion export exclusion landed in src/lib/db/export.ts; two Opus agents running: companion wiring (six slots + e2e/companion.spec.ts) and ensureCard for every itemKind (quality-bar item 1). FM2 batch A closed by the sibling (105/0); q0018(a) false origin: 2 of 2 with the warning. 01:55: FM2 pre-read 1 (displacement-time, average speed) sent (docs/dev/qa/pre-read/fm2-a-1.md, 7 findings incl. the km/h judgement call); graph common errors on plotted-point parts now fire when the test names the misplaced points (graphTestMatches reads the plot JSON; 41 transformation tests green); M8 lesson-v2 pass landed on all 15 notes (authored heroes). 03:25 LANDED: the QWC surface for text-long parts (src/lib/marking/qwc.ts evidence engine, LongTextField textarea with word count and a minWords gate, mark.ts text-long case returning the evidence summary and the honest band floor with decision "qwc-band", QuestionRunner QwcDecision panel: evidence checklist with quoted hits, band descriptors as radios, mark stepper within the band, one "Award N marks" button recording tag qwc-self-banded; SolutionDetails now also shows a mark point’s examinerNote; 650 marking+items tests green; verified live on b1-aerobic-respiration q0009 via ?exam=2). Only the three drawing parts remain paper-marked. 03:15: engine fixes landed (text common errors keep the key-word marks; fixMatches treats a hyphen between letters as a space; 40 tests green, tsc clean); FM2 pre-read 2 (velocity-time, constant acceleration) sent (docs/dev/qa/pre-read/fm2-a-2.md, 13 findings, numbers clean); B2 blood/heart pre-read launched. G7 QUEUE (02:55): FM3 A filed (mean-and-standard-deviation, mean-sd-grouped-data, combined-sets-mean-sd, adjusted-data-mean-sd; glance at the adjusted-data swap-line figure requested); pre-read c running; FM2 A ×2 running; B2 A (osmosis, transpiration, blood-and-vessels, heart) launching; P2 A (waves, echoes, EM spectrum, reflection) and C2 A next; exemplar v2 notes to read. 02:35 LANDED: table, steps and label answer fields (TableField, LabelField, steps via OrderField/stepsAsOrder; markers src/lib/marking/table.ts and label.ts with named targets so a diagram label never leaks its answer; mark.ts shareMarks helper; 36 parts off paper; 631 marking+items tests green; preview-verified on B1 cells-and-microscopy and photosynthesis-investigations). Remaining self-marked: text-long (QWC, ~29 parts) and drawing (3). 02:50 LANDED: PracticeFlow (src/components/topic/PracticeFlow.tsx + practice-sets.ts + ux/CloseCard.tsx): one question at a time with a ProgressLine and Next question, Fix it now → the topic’s first worked-example twin inline, then the authored sets (q/dx/rp/ftm resolved by id, empty state for topics without sets), then the close card (marks, minutes, fresh stones, Saved on this device, Next topic / Back to the lesson); position persists in the flow row (practiceIndex/examIndex/setIndex) and a reload resumes; ReviewComplete now uses CloseCard; 790 tests green; preview-verified on three topics. Deep link added: ?practice=n / ?exam=n (1-based) opens that question and becomes the stored position; e2e plot and transformation specs now find the practice index from the published JSON and deep-link (rebuild + rerun pending until the hero and QWC agents finish editing). Hero/spine agent (Opus) launched: TopicHero from the hero block, spine of numbered sections with minutes, eyebrows as verbs with minutes, Literata self-hosted. 19 Sep 23:40 LANDED (agent; suite 1011, tsc 0): re-teach on an unrecognised miss (src/components/topic/reteach.ts + 24 tests; QuestionRunner ReteachPanel: 'Where the next mark is' with the first unreached scheme step and, where unambiguous, the matching worked-solution line; 'Another way to see it' from the first hint or the solution; Try a twin primary, Try again secondary, nothing auto-revealed; second miss adds the unsigned support line; exam-style shows it only after the run under 'What to pick up'; recognised common errors unchanged). Mangled-regex lint fatal in content-lint ([sS], [dD], [^;n], (?![sS], b( signatures; content:check 117/0). Sheet section eyebrow 'The reference for this topic' (sibling's panel ruling: the closing note panel becomes a pointer, the Sheet is the single home of the traps). Usage: the sibling's counter read 57% at ~23:35 and rising ~0.5%/min, so 80% lands near 00:10 tonight, not 02:40: no more launches; the three pre-read agents told to file partial reports within 15 min; the export rebuild (build5) + six e2e specs are the last compute. 19 Sep 23:33: C2 re-read filed (c2-a-reverify.md, partial: reactivity/displacement hold; redox has 8 name-the-substance parts paying the wrong substance + Cu2+ unaccepted; rusting 4 findings) and forwarded; engine: the ce unwrapper is brace-aware, so the notes' $\ce{Fe -> Fe^{3+} + 3e^-}$ marks (equation suite 33). 19 Sep 23:30 CLOSE (usage 62% at 23:29 by the sibling's counter, rising ~0.8%/min; stopping ahead of 80%): fifth build good (build5, out/index.html 23:24, all three content folders); e2e alone on it: companion 8, learn 12, practice 4, papers 4 (spec now waits for the plan cards), plot 8, transformation 4 green; today 6, first-run 4, flashcards 6, map-ledger-settings 8, offline 8 green on the fourth build and their files are unchanged since. Unit suite 1011, tsc 0, content:check 117/0. Re-teach panel verified live (frustums q1). Pre-reads forwarded: FM1 C (fm1-c-1.md: trig-graphs marks its own 0.64 wrong; show-that parts pay working backwards) and FM1 D (fm1-d-1.md: stationary-points pays the target misconception; tangent-slip texts backwards); the C2 re-read agent was told to file partial (c2-a-reverify.md) and stop; resume it by SendMessage if it was cut. The sibling holds all fix passes until the window resets. STATUS at the 19 Sep close, against DeepStash / Seneca / Bitesize / Corbettmaths + past papers (honest): BETTER: every question is original in CCEA's layout with the scheme in M/A/MA language and its verification shown; a topic opens on a hero (lede, three 'can' lines, honest minutes, the note's own figure) with a spine that names where she is and where she can pause; every miss now comes back in her reviews (cards for every item kind, backfilled), an unrecognised miss shows the scheme step she has not reached and offers a twin, and the review inbox re-asks questions, gates and mistakes with their figures; the papers page is her eleven entries on their real dates with raw → UMS → grade, which nothing else does for CCEA; 117 bundles pass a strict build that now catches leaked values, undrawn figures and mangled regexes; the companion speaks at seven moments and never inside the work. EQUAL: flashcards and retrieval prompts (Anki/Seneca do this as well); the note prose (Bitesize is shorter, we are deeper, neither is prettier yet); offline copy. BEHIND: no method marks yet (2,005 authored MA marks never awarded: the WorkingLadder is the biggest remaining gap); first run still ends on Today rather than inside a lesson (Duolingo/Kinnu); the expected answer is printed before 'Try again' so the re-ask is hollow; the companion's line budget makes it go quiet after three topic opens; C1, P1 and Unit 7 have no lessons (content side); no timing data behind the minutes; the look is competent, not yet DeepStash-grade (appearance pass B pending). NEXT (19 Sep 23:20, clock verified with `date`; the four stamps before this one in this segment were estimated and run about two hours fast): (1) re-teach agent lands → tsc, vitest, rebuild with the exit line checked, e2e learn/companion/practice/papers/plot/transformation alone; (2) forward the FM1 batch C pre-read (fm1-c-1.md) after verifying its marker claims; (3) first run verified live on a cleared dev profile 16:05: note → Letter + name + plan → seeded B1 cells lesson (hero, section 1, gate answered, caption, Continue, marked label question with the scheme caption) → Skip to Today; chrome-free. Finding sent to the sibling: b1-cells q0001 figure prints its own answers. quality-bar item 7 LANDED 16:20 (agent; suite 1030, tsc 0): first run ends inside a seeded lesson (SeededLesson.tsx: hero lede + one can line + figure, the first section through its first gate with the check caption, the first practice part marked with the scheme caption, a close that names the stone rule and links into the topic; Skip to Today on every step; chrome-free /welcome via Nav.isChromeFree; firstRunDone written before the slice; e2e/first-run.spec rewritten, completeFirstRun clicks Skip to Today). Seed on a fresh profile: science.b1.b1-cells-and-microscopy. Left: the continue link lands on #note (headings carry no ids), the stone is never placed on the night by design. Original: quality-bar item 7, STARTED (agent stopped clean at the 80% mark, 01:30): src/components/gift/seed.ts + seed.test.ts landed (seedCandidates from the soonest paper in teaching order, chooseSeedTopic with an injected hasHero predicate, FALLBACK_SEED histograms; no importers yet; suite 1018, tsc 0); NOT started: the chrome-free lesson slice in FirstRun, the three captions, Skip to Today, firstRunDone before the slice, e2e/first-run.spec + completeFirstRun. Resume the same agent by SendMessage with the remaining brief. Original brief: first run ending inside a seeded topic on a chrome-free surface (FirstRun.tsx + a seeded lesson slice; the Letter now sits in FirstRun step 1); (4) item 2: WorkingLadder method marks (MarkPoint.check, requiresWorking, fixMatches), opt-in on lessons, default on exam-style; (5) re-read requests from the sibling: C2 fix pass, B2 pair-2 fix pass (B4/H5 parts back to d.p.), FM1 C after its fix pass; (6) group 3 remainder (findInBundle/ReviewInbox done; ensureCard done), group 5 (timing data, UMS progress view, run-up-to-papers on Today), photo fallback for the 3 drawing parts; 20 Sep 01:27 LANDED: FeedbackCard withholdExpected, passed by QuestionRunner as reteach && partMisses < 2, so a first unrecognised miss no longer prints the expected value above Try again (tsc 0; topic+items suites green). Window: counter 73% at 01:19, reset 03:30; both agents given a hard stop at 01:35 (partial reports); engine queue waits for the reset. (1b, remaining) re-teach follow-up: the panel renders as briefed, but FeedbackCard still prints "Expected 565 cm³" on the first miss, above "Try again", so the re-ask is answered before it is asked; withhold the expected value on an unrecognised first miss (show it after Try again, the twin, or "Worked solution and mark scheme"), and make "Another way to see it" prefer a re-presentation over a pointer hint ("The cone formula is on the Higher formula sheet" is a pointer). (5c) C2 A second pass LANDED 01:24 (redox 24bf62711fd715a5, rusting 97419db1f0df0f99; R1-R8 and S1-S4 applied with three narrower rejects where the literal ones cancelled accepted answers): re-read after the reset. Engine from it: normaliseText should fold a superscript ion charge to the plain spelling (Cu²⁺ and Cu 2+ read as cu2+, SO₄²⁻ as so42-) so authors key one spelling. (6a) algebra `form` values "single-log" (exactly one \log in the response, no + or − between log terms) and "expanded-logs" (no product, quotient or power inside any \log), marked form-then-equivalence like "y=mx+c": the marker already treats log expressions as equivalent under the log laws, so laws-of-logarithms is held on the content side until this lands; also tell authors that check scripts must pass `prompt` (not `stem`) to markAnswer or accuracy instructions are inert. 20 Sep 16:05 LANDED: log forms single-log / expanded-logs (algebra 101, schema exported, content:check 132/0; laws-of-logarithms released); ion charges folded in key words (Cu²⁺ = cu2+); tuple tolerance on coordinate specs (algebraic `tolerance`, points message reworded); one-letter units with a slash, power or per (g/s, s⁻¹, g per s, s^-1, g s⁻¹). QUEUED reads: C2 B (measuring-rates 17e89f9f5be315b2 / 00af7a415ac01d5f, collision-theory c33c0c094d67cf23 / 0305ec439bd216b0, equilibrium 76b695a8bee8e294 / 657f10175e57054d, energy-changes 463ce29f65332b63 / ce7e9c6f602a8afb) and matrix-arithmetic (00610f934d77f88d / 82102725896bdec4). 15:48 (clock verified) matrix answer kind agent launched (schema kind matrix, src/lib/marking/matrix.ts, MatrixField, lints, README rule); six agents running (FM2 B, FM1 E, FM1 F, B2 B pre-reads; first-run build; matrix kind). 15:45 LANDED: vector spellings (vectorInput: bold/hat/underline unit vectors, pmatrix/bmatrix columns, tuples, trailing units; i/j sampled as e_i/e_j so a wrong vector gets an algebra diagnosis; algebra 104) and paired-answer common errors on the assignment spelling (mark.ts pairNames). 21:25: seventh build e2e all green (plot 8/8 alone; loop: first-run 6, today 6, learn 12, companion 8, practice 4, papers 4, transformation 4, flashcards 6, map-ledger-settings 8, offline 8). FM2 D read in (fm2-d-1.md, ten findings + one app-side): engine fixes landed: a direction word after a numeric answer is stripped before the unit is read (DIRECTION_TAIL_RE), find-the-mistake lastNumber ignores unit exponents and prepare drops mechanics units. 21:10 (clock verified): the user lifted the stop rule in this session ("continue do not stop"): both sessions run to the limits; cut agents resume by message after the reset. Launched: appearance pass 1 (tokens and type, on the amended direction with 04-critique prevailing; e2e/appearance.spec.ts) and the FM2 C pre-read (fm2-c-1.md). Agents out: FM2 D afc13c194592008c5, logs re-verify + matrix-equations a580f9149ad8aa545, FM3 B a54edb066c8d9fefa, appearance pass 1, FM2 C. Earlier note: FM2 C READY for pre-read (force-diagrams 5ac525fc794801b4 / fc00e84cfa7a721c, resolving-forces c1e2f498e3b3a687 / fcab944f74db4ee8, resultant-of-forces 9fe923dab061a5d8 / b89259c3eef544ee, equilibrium-of-forces 0dc895a9f97895f1 / 09623c5fab6962da; launch fm2-c-1.md after the reset with the FM2 D pair). scripts/qa/figure-leaks.mjs is in (LEAK gates, REVIEW does not); make it fatal in content:check when it and my FIGURE line both print 0 and the REVIEW tier is reconciled. The sibling relays that the user withdrew the stop rule in its session; mine stays at 80% until the user says so here. 21:55: seventh-build e2e so far: first-run 6, today 6, learn 12, companion 8, practice 4, papers 4, transformation 4 green; plot 6 of 8 with two page.goto ERR_ABORTED 60 s timeouts under load (five agents + builds running): rerun plot alone after the loop ends before calling it a regression. 21:50: B2 B re-verify forwarded (b2-b-reverify.md: all 33 hold; three small opens: fertilisation q0013 QWC bare-noun keys, contraception q0001 hormone-level spellings, a duplicated trap). Overlap lint widened to 8 px anchors. Remaining agents: FM2 D afc13c194592008c5, logs re-verify + matrix-equations a580f9149ad8aa545, FM3 B a54edb066c8d9fefa. 21:40: matrix pair pre-read forwarded (fm1-g-matrices-1.md, twelve findings; MI-4 mine: MatrixField now has a scalar box in front of the brackets and text entries everywhere, submitting "1/5 [2 -1; -3 4]"; matrix suite 28, tsc 0). e2e on the seventh build so far: first-run 6, today 6, learn 12, companion 8 green (rest running). Sibling counter 70% at their 22:15 (my clock runs ~38 min behind theirs); 80% expected near their 22:35: no new launches; the four running agents (B2 B re-verify, FM2 D, logs re-verify + matrix-equations, FM3 B) resume by message after the 01:40 reset if cut. newtons-second-law-inclined-plane published (639d6da46656e3cd / dd8e0efb31799a41) for the FM2 D read. 21:25: P2 A shingle + figure pass landed (wave-types adede8cbafff1873 / 19131a20e6982eeb, echoes e0f9bbdcee48158a / 49898d429440023d, em-spectrum a1f962e00bb20cd7 / f75b42a915e9bc72, reflection c3515eaf68b31066 / 82e55bcdd696ea9e; text only) — no read needed; P2 C and D and FM3 D being written. 21:20: seventh build good (tsc 0, build 0, out/content complete, index 20:57); all eleven e2e specs running alone against it (e2e-run5.log). Agents out: matrix pair a09e771a18bc10253, B2 B re-verify ad41ba3c89fd895ee, FM2 D afc13c194592008c5, logs re-verify + matrix-equations a580f9149ad8aa545, FM3 B a54edb066c8d9fefa. 21:15: art-direction critique filed (docs/design/art-direction/04-critique.md): appearance pass to implement the direction as amended (five amendments, three cuts, fourteen Playwright checks; the data-subject attribute must move to the page root and the dark subject rules restate lightness before tokens.css goes in; hero.short exists in no bundle). Pass 1 (tokens and type) launches as an agent when the amended direction lands. 21:10: FM3 B pre-read launched (fm3-b-1.md; addition 13bb8393d70b2b68 / 585c295506f1cb3c, venn 1898b1f2fda468dd / d359e30e9a9613a0, tree 106d881596dd7b65 / 75d5da5ee1650adc, conditional e0de9ba4b02ca650 / 5a8117441b990bbd; probe the dp-tolerance claim). 21:00 LANDED (agent; suite 1094, tsc 0; verified live on frustums q1): the WorkingLadder (src/lib/marking/working.ts methodSteps/markWorking, WorkingField.tsx, QuestionRunner ladder on Check, FeedbackCard seenMarks, Attempt.workingRaw; accuracy step withheld on a first miss). Quality-bar item 2 is in: method marks are awarded. Seventh build running for e2e. STATUS delta: 'no method marks' and 'expected answer printed before Try again' move from BEHIND to done; behind now: appearance (art direction filed, critic running), companion line budget, C1/P1/Unit 7 lessons (content), timing data. 21:15: FM1 E (fm1-e-reverify.md: all hold; optimisation q0015 label overlap; three WE figures leak) and FM2 B (fm2-b-reverify.md: all sixteen hold, publishable) re-verifications forwarded. Engine: vector unit tail stripped before unicode normalisation (m/s² in every spelling); figureLeakWarnings walks worked examples and names overlapping labels; content:check 154/0 with 143 FIGURE warnings. Still running: matrix pair pre-read, B2 B re-verify, FM2 D newtons read, WorkingLadder (resumed). 21:05: C2 B forwarded; engine: wrong-form partial credit (equivalent-wrong-form / not-simplified keeps marks − 1 on multi-mark parts; mark.ts + README); reciprocal and worded units unified in looseUnitKey; doubled-backslash lint fatal; numeric-with-unit figure leak warned. Ready for re-read: laws-of-logarithms fix pass 95f13359b98eeb21 / e3eced6ce8c25ea9; matrix-equations published bdab5f28f040702e / 49cff3ed999575b5 (read with the matrix pair once that report is in). 20:50 (clock verified): the weekly cap cut four agents at 16:50 (matrix pre-read, FM2 B and FM1 E re-verifies, WorkingLadder, the last mid-fix on the Expected/accuracy-mark coherence); all four resumed by SendMessage after the 17:00 reset; C2 B pre-read had completed (c2-b-1.md: all four hold; a volume-time curve drawn flat at every reading, units in words against symbols in specs, a reject cancelling its own key words, reversed answers scoring, double-headed profile arrows, a doubled backslash in a recap). Engine from it: unit words agree with symbols in the loose key (grams per second = g/s, per second = s⁻¹ = /s), a doubled backslash inside maths is a fatal lint, a numeric answer with its unit in a figure's prose is a FIGURE warning. New agents: B2 B re-verify (b2-b-reverify.md), FM2 D topic 1 pre-read (fm2-d-1.md; newtons-second-law-linear 3ca3d4b0484d20a1 / 5a70a573e3055dbc). Ready for re-read later: B2 B fix pass hashes repro 2822497f6597b7f1 / 9a59f41fd86b4f09, fert 1f727e669b58c803 / 6368e68e57b5e2d9, cycle 21df5e358a961806 / d78c53c0ab95ca96, contra d84a2a7ceaca7759 / 169f8d52c42c85f4; FM1 C shingle pass trig-graphs 7c6f9ccf2730cd8f, trig-equations 14f3e7f5d3c245e3. 16:55: laws-of-logarithms pre-read forwarded (fm1-g-logs-1.md). Engine from it: "4 or -4" is not the number 4 with a unit (a standalone number or or/and in the unit tail makes the response unparseable); a fraction before a function is its coefficient (1/2 log n); fixMatches' value fallback needs a stated value; form single-log-expanded (argument multiplied out). Art direction filed by the sibling under docs/design/art-direction/ (00-audit, 01-art-direction, tokens.css, 02-surfaces, 03-implementation-plan with three passes): the appearance pass launches as an agent on pass 1 after the WorkingLadder lands and the weekly reset. FM1 F fix pass LANDED (area-under-curve 93072c8ed1ea0ce2 / 29377df6a0f3bb59, logarithms-from-indices e112645bda80bcf8, log-log-graphs 8825b1f829632f6b, indicial-equations 8e545e101ffa34ad / 6f81d5e4478e51b0) and laws-of-logarithms 7bc10147a9d61be0, matrix-arithmetic 66035de2e365335e, matrix-inverse-2x2 2a4eede08f5b18c0 (scalar sentence gone). shingles.mjs: 561 breaches corpus-wide, rewording passes running unit by unit; strict-build candidate at 0. 16:45: FM2 B fix pass LANDED (vertical-motion 2b06c4707bfee5ac / 616f904e5171d4a2, vector-scalar 0846cd0a570602ba / b9764105a4307cd5, magnitude-direction 8bfd66a906b7bb6b / 750be20f091f7720, ij c6b3107eebf50a01 / 6b799e0d6f175435; q0007 split into 0007 + 0008): re-verify after the reset. Engine: vector common errors match every spelling (mark.ts passes the part's variables); unit tail stripped before tuple/column parsing. C2 C part 1 READY: c2-homologous-series-alkanes 2b1aa1bd460f3638 / 6ce1a88eec7c20a7, c2-crude-oil-fractional-distillation 68afa0d865fc89c3 / 92d993f1231745ba (label, steps, table, equation parts); combustion and alkenes to follow. lesson-v2: allow-list file lesson-v2.allow.json; 14 warnings left. 16:35: matrix reads READY: matrix-arithmetic converted to the matrix kind (bundle 6b4b47e7c86723b3, note 2f478706c80f1e9d) and matrix-inverse-2x2 published (e8bf8baf1dadfdd7 / 22c427d9556d7f4b); pre-read fm1-g-matrices-1.md to launch after the weekly reset with laws-of-logs in hand. Engine: parseMatrix multiplies a leading scalar into the entries (\frac{1}{5}(…), 1/5 [[…]], 0.5(…), -2(…)), so the stems' "a fraction in front is not read" line can go. 16:25: FM1 E fix pass LANDED (curve-sketching d329633fccd2be70, optimisation 4f6dab8cffab594c, integration-as-inverse 84df2583de8ea4a9, definite-integrals 90435a53c31ad368); re-verify agent launched (fm1-e-reverify.md). content-lint: a numeric common error on the correct value is fireable when the part has unitRequired (unit-omitted errors may be numeric again). 16:15 (my clock; the sibling reads the weekly cap at 93%, binding ~16:45 before its 17:00 reset): agents that may be cut and must be RESUMED BY SendMessage after the reset: C2 B pre-read a7184b494219219ea, laws-of-logarithms pre-read a702b85c2f5f7533f, WorkingLadder a6ff5bbdeedfaf365 (told to keep the tree compiling between edits; check tsc first on resume). Also running: FM1 E re-verify ad0d5d1f39732c8a9, FM2 B re-verify a6b774133269635e9, matrix pair pre-read a09e771a18bc10253 (fm1-g-matrices-1.md). 16:50: content-lint gained figureLeakWarnings (a question figure printing a phrase a part asks for; a FIGURE warning in the strict build, counted in the summary, letter labels exempt); content:check 140/0 with FM2 B's backslashes fixed on the content side; lesson-v2 0 breaches in the strict run; the sibling's scripts/qa/figure-leaks.mjs (exit 1) is the one to make fatal when it prints 0. 16:40: B2 B pre-read forwarded (b2-b-1.md: five question figures print their answers, the presence-based class again, ICSI photo off-spec). 16:20: FM1 E (fm1-e-1.md) and FM1 F (fm1-f-1.md) forwarded; engine from them: the listing rule keeps a bracketed pair together (countListedItems masks commas inside brackets), the constant of integration in either case (+ C earns + c); lesson-v2 breaches now fatal in content:check (package.json). 16:10: sixth build e2e, every spec alone: first-run 6, today 6, learn 12, companion 8, practice 4, papers 4, plot 8, transformation 4, flashcards 6, map-ledger-settings 8, offline 8 (74 green) — the seeded-lesson ending and the new completeFirstRun helper hold. FM2 B pre-read forwarded (fm2-b-1.md: vertical-motion publishable; vector-scalar, magnitude-direction and ij hold; 13 lost backslashes, a reversed answer scoring full, a common error whose value does not follow from its own description). Lost-backslash TeX lint added to content-lint. LINT queue: lesson-v2 breaches are fatal in content:check (done); promote traps with --traps-fatal when the sibling reports 0 warnings (31 at 16:10 after the traps pass: 11 B2 B staged, 16 for an allow-list, 3 mis-citations, 1 missing block); lift the sibling's figure-leak check (SVG text/title/desc against a part's answers) into content-lint when its B1 pass lands. 15:55: sixth build good, all eleven e2e specs running alone (e2e-run4.log); C2 B pre-read launched. 16:30 LANDED (agent; suite 1064, tsc 0, schemas exported): `matrix` answer kind (src/lib/marking/matrix.ts, MatrixField, mark.ts cases, two fatal lints, README rule; spec {kind:matrix, rows, cols, entries, tolerance?}; common-error pattern {kind:matrix, entries}); FM1 G's six table-encoded matrix parts to convert on the content side, then read matrix-arithmetic with laws-of-logarithms (a8949616be663029 / c7048f80f8d08f74). WorkingLadder agent launched (src/lib/marking/working.ts + QuestionRunner working textarea; method marks from typed lines via stepLineMatches/solutionLineFor). Agents running: B2 B, C2 B pre-reads; WorkingLadder. ENGINE (done): matrix kind (pmatrix/bmatrix latex parsed to a matrix value, entrywise feedback, commonError matrices) so FM1 G stops encoding matrices as table specs. (6b) engine items from FM1 D's author: an optional absolute `tolerance` on tuple specs (algebraic latex that is a tuple: schema + spec-map + mark.ts passing it to checkPoints, whose verdict text for a plain coordinate answer must stop saying "does not satisfy both equations"); the simplest-form check must treat 12/x^4 and 12x^{-4} as the same form (algebra.ts not-simplified path) before authors may use equivalence "simplifiedOnly" on derivatives; (6c) G7 reads queued: FM1 batch E READY (curve-sketching-quadratic-cubic, optimisation, integration-as-inverse, definite-integrals; follow-up landed, 124/0, prompt key live; note hashes bee865d2721a56f4 / 0605dc138960c547 / faf64d5e094f2968 / 564661a941e0f38b; bundles b69baa8c761305fc / dfa8263997a2c8c0 / 761cdbc477ea82b7 / b9d97ff546b6deea; curve sketching has nine gates by the gate-per-section rule, counts are guides not caps): pre-read fm1-e-1.md; then FM1 F part 1 READY: area-under-curve (1fd672dcb0fce17e / 795ab749e1bdf4ab; crossing-region case under a notonspec, recurring-decimal areas as exact fractions) and logarithms-from-indices (d7067ae02e6c8655 / a24aaa46b4e12693) for a pre-read fm1-f-1.md; log-log-graphs READY too (7da3f7f2e873b235 / 162dfdabe2366491; has an interactive points-line plot part, check its lattice and tolerance against plotLattice and the PlotField); indicial-equations READY (6439df193cdfdd3a / ca73f5bc38101654; same-base parts method-locked to powers of 2); FM1 F read is now four bundles (area-under-curve, logarithms-from-indices, log-log-graphs, indicial-equations); laws-of-logarithms held on my single-log / expanded-logs forms; B2 batch B READY for pre-read (b2-b-1.md): b2-reproductive-systems 18a74c1873497522 / adb36da347a2801a, b2-fertilisation-pregnancy 959d90770a8bbf80 / be20b06a1a3f1895, b2-sex-hormones-menstrual-cycle c61ae5fc16c79f52 / 2545f552638736a7, b2-infertility-contraception 733b70285e523733 / 1c9f09317002afe4 (128/0; read the menstrual-cycle videos against the FSH/LH exclusion, the QWC rubric wording, the four photo credits); FM1 C re-verify FILED partial at the 80% mark (fm1-c-reverify.md): every finding holds and is proved under the marker (the sin 40 parts, the show-that rejects, 0003, the find-the-mistake blame, 0012(a)/0013(d)(i), 0011, 0010(a)/(c), the rewritten gates, 0009(c), the eleven rescues gone); spec ids live under statementIds in data/spec/further-mathematics.json and all match. Still open for next window: figure SVG-vs-alt on form-three, trig-graphs and trig-equations; the 53-71 word panel claim and sheet.traps presence; trig-graphs gate g8 dropped in the restructure: the sibling confirmed the tan has-no-maximum fact survives in the teaching body, only the check is gone (gate decision left to the lesson pass); the degree-sign spellings proved on trig-equations 0001 only, re-mark the other ten solution-set parts. FM1 C fix pass LANDED 23:41 (all eleven findings applied; hashes in the sibling message of 19 Sep; its check-findings.mts runs my probes and passes): re-verify next window with the marker plus the two checks my pre-read left open, specRefs against data/spec/further-maths.json and SVG-vs-alt on form-three, trig-graphs and trig-equations; FM1 D fix pass LANDED 01:3x complete (all five findings; nature parts keyed value-then-conclusion with min/max; constant-slip errors derived from the arithmetic; panels 62-70 words; hashes in the sibling message of 20 Sep): re-verify next window with the marker plus my pre-read partials (SVG-vs-alt on the 38 figures, the marksTypicallyEarned round-trip); FM2 batch B READY for the G7 read (panels converted 01:33; note hashes 632e0cc8b287cc29 / d326065ef20ebd39 / c0ba72ede354f706 / 3c0b1e83f10d0ae2; bundles ed06178ba21e6fbe / 5a08bf9a1770927e / 06dbb79038f4cf10 / 07ae92c3c61f1234): pre-read fm2-b-1.md, four bundles, mechanics with g = 10 (9.8 in notOnThisSpec), vector i/j answer lines carrying the typed-form instruction until the engine items land. Engine items from FM2 B: (i) i/j vector answers must accept the bold form (mathbf i + mathbf j), a pmatrix column vector and a unit typed after the expression, so authors can drop the typed-form instruction from every answer line; (ii) a paired-answer commonError must match the assignment spelling (p = 2, q = -3) as well as the tuple (2, -3), since the part accepts both as correct; (7) honest status report better/equal/behind against DeepStash/Seneca/Bitesize after the sister's next sitting. Stop at 80% of the 5-hour window on the user's word (window opened ~22:39 on 19 Sep).
- (older queue, superseded where it overlaps) NEXT WINDOW QUEUE: (0) FIRST, before any G7 reads: the learner test. The sister tried the platform on 13 Sep: appearance not very nice; liked the content, videos and interactive questions; disliked the pre-check gate before anything; ignored paper-only questions; liked the ordered topic list but found a topic page badly structured, intense and unattractive for a first-timer. The sibling ran a nine-agent review; the verified plan is docs/plan/review/2026-09-13-learner-experience-review.md (section 7 has the order of work). My layer, in this order: (a) delete the pre-check lock in TopicContent.tsx:80 (opacity-40 / pointer-events-none / aria-hidden until the confidence pre-check is done), cut the pre-check to 3–4 optional prerequisite items, add a post-instruction check, persist flow state, remove the item.skill leak in DiagnosticWithConfidence.tsx:119 and the false "Sheet highlights what it found" line; (b) delete the QuestionRunner.tsx:30 clause that sends text parts of 2+ marks to paper (375 parts / 1,037 marks; 309 convert with no authoring) and ship an "award yourself the mark" override in the same commit, then TableField/StepsField/LabelField (36 parts), a QWC surface for the 27 text-long parts, fixMatches in faded steps, and a WorkingLadder using MarkPoint.check for the 1,827 method marks the UI never awards; (c) topic page v2: hero with lede, figure and one button; numbered sections with a ProgressLine spine; worked example inside the note; one-question-at-a-time practice rendering bundle.sets; Sheet collapsed as the closing "In the exam" panel; reviser path on an explicit flow flag; fade order full → faded1 → faded2 → twin → problem; ensureCard for every item kind and onTwin wired; (d) appearance: three surface levels instead of one card recipe (56 call sites), type-scale tokens (h1 32, h2 24, prose 17–18), subject tints via data-subject, section eyebrows as verbs with minutes, a mobile "More" destination, theme default system. The sibling migrates the two exemplar topics (b1-enzyme-factors, frustums) to its Lesson template v2 once (a) and (b) land, then the other 90. UPDATE 14 Sep 02:50 (via the sibling): the user approved the whole review plan and added two asks: a Duolingo-grade companion character that builds a relationship and makes her want to come back, without manipulation (emotional-design rules stand; rule 2 "mascot" wording may be reworded if argued honestly), and many agents led by Fable 5.1 with Fable/Opus subagents, moving fast (the 80% window rule still applies). The sibling is running research → concept → judges → spec → critic for the companion and will build it as a self-contained module (src/components/companion, src/lib/companion, data/companion) with the spec in docs/plan/companion/; it sends me the integration contract before touching my files, and I wire it into Today, FirstRun, TopicContent and the close card. UPDATE 14 Sep ~03:30: the sibling has already done the first half of (a) in src/components/topic/TopicContent.tsx (it now treats that file as its own for the lesson-flow work): the lock and dim are gone; section order is Lesson → See it → Worked examples → Check yourself (diagnostics after instruction, visible Skip to practice, honest done line) → Practice → Exam-style → Find the mistake → Retrieval prompts → In the exam (the former Sheet, id "sheet") → Where marks are lost; nav Lesson / Examples / Check / Practice / Exam / Mistakes / In the exam; fade order full → faded1 → faded2 → twin → problem; tsc clean, 723 tests. CONSEQUENCE FOR ME: every e2e helper that clicks "Skip the pre-check" (e2e/plot.spec.ts openTopic, e2e/transformation.spec.ts firstGridForm, learn.spec.ts, scripts/qa/crawl-export.mjs) must change to the new section (id "check", button "Skip to practice" or none needed since nothing is gated); verify on 3017 first. Still mine from (a): cut the check to 3–4 pre items plus a post-check renderer keyed on `when`, persist flow state and gate answers, the item.skill leak in DiagnosticWithConfidence.tsx:119; then (b) the QuestionRunner paper clause, (c) the hero, (d) appearance. Item (e) of my queue therefore: read the companion spec when it lands, review the contract against the emotional-design principle, then wire it. Then (1) rebuild the export (npm run build) and run the full e2e suite alone; (2) verify the three pre-read reports and send one consolidated B1 part-3 verdict (also confirm blood-glucose q0004 a(i) accepts "glucose in urine" without "the", and respiratory q0001 is four 1-mark mcq parts: both seen on disk); (3) G7-read the M3 handling-data batch (quartiles-and-IQR, cumulative-frequency, box-plots: first published plot "box" parts, add the e2e case); the NaN fix landed at 14:14 (content:check 86/0 per the sibling, not yet rerun here); (4) FM1 batch A, final report filed 14:20 (algebraic-fractions-simplify, -multiply-divide, -add-subtract, expand-three-brackets; schemes M/W/MW; content:check 89/0), then FM1 batch B, final report filed 14:25 (completing-the-square, completing-square-minimum-point, completing-square-solve-surd, quadratic-inequalities; FM1 now 8/29 published) plus FM2 displacement-time-graphs and B2 osmosis as the sibling files them; (5) act on the grid-fields audit (docs/dev/qa/grid-fields-audit.md, delivered 14:12): must-fix G1 first (PlotField snaps to major/5 while five published parts have targets finer than that, e.g. a 0.15 bar on a 0.1 lattice and (5, 1.6) on a lattice of 1, so the right answer cannot be tapped: refine `minor` to the coarsest of /5, /10, /20 that puts every target within tolerance, raise the y niceStep target, and add a content-lint check), then K1 (typed route for the two-point line), V1/V2 (guide and axis-number contrast: accent-2 strokes and 5.8 px labels), C1/C3 (feedback wording: drop "wrong", give the line miss a direction), T1 (commit on pointerup with drag-to-adjust), and the should-fix list (aria-live hint, stale-parse submit, bar chosen from the unsnapped x, degenerate line, unique clip ids, keep the grid mounted under the feedback); the three B1 pre-read agents were stopped at 14:13 to honour the 80% rule and can be resumed by SendMessage in the next window; (6) the sibling stopped its five authors at 14:15 (build 92/0): filed for G7 are M3 handling data (3), B1 part 3 (6), FM1 A and B (8); on disk but unreported and NOT yet G7 candidates: FM2 displacement-time-graphs, average-speed-and-velocity, velocity-time-graphs (constant-acceleration-formulae unfinished) and Year 12 science partials b2-osmosis, b2-transpiration-potometer, c2-reactivity-series, c2-displacement-and-extraction, p2-wave-types-and-properties, p2-echoes-ultrasound-sonar-radar; the sibling resumes its stopped authors in place first next window; (7) the sibling's stop report lists the final on-disk bundles.

## Open
- Misconception registry holds 202 entries; from 13 Sep authors add any missing entry to pipeline/mine/insights-source/*.mjs themselves and finish with `npm run insights:unregistered` + `npm run content:check` (sibling's briefs updated).
- The desktop app's in-app browser pane cannot register the service worker ("An unknown error occurred when fetching the script"); that is a pane limitation, not an app bug. The Playwright offline spec (real Chromium) is the proof that sw.js installs and serves offline.
- Sibling delivers: M8/M7 H bundles, S/L batches, FM1, science units, decks FM1–3/C1/C2/B1/B2/U7/M7/M8; media map (data/links/media-map.json).
- Owner decisions: her entry shape (Year 11/12), name (default Cairn), deployment (local vs Vercel).
