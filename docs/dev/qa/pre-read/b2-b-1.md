# Pre-read: B2 biology set 1 (reproduction)

Four bundles, read against CCEA Double Award Science B2 §2.3 in `data/spec/double-award-science.json`
(2.3.1–2.3.7), the marking engine in `src/components/items/mark.ts`, and the figures' own SVG text.

Everything marked **verified** below was re-run with a script after the fix was applied in memory, so the
"after" numbers are measured, not predicted. Scripts are in the session scratchpad
(`b2b1-mark.mts`, `b2b1-verify.mts`, `b2b1-verify2.mts`, `b2b1-verify3.mts`, `b2b1-num.mjs`, `b2b1-bez.mjs`,
`b2b1-figs.mjs`, `b2b1-scan.mjs`); digests are beside this file.

Clean across all four, checked and found nothing: no unpaired `$` or `${`, no `undefined`/`NaN`, no
exclamation marks in prose, hero first with a lede and exactly three `can` lines and `minutes`, every
closing panel under 80 words (70 / 61 / 67 / 64), every teaching section between gates under ~150 words
(the one exception is 151), every prompt id resolves, part marks sum to `totalMarks` and scheme marks sum
to part marks everywhere, every MCQ and diagnostic has exactly one correct option with no duplicate texts
and feedback on every option, every `whyMenu` has a valid single correct index, all four photos carry an
open licence, a credit and a source URL (both "Public domain" entries verified on Wikimedia as PD-self and
NIAID-PD; the other two are CC BY-SA 3.0 and CC0, and every credit matches the source page), all four
YouTube ids resolve to the title printed in the block, and every number in every bundle recomputes
(`b2b1-num.mjs`: 48 checks, one failure, listed under the menstrual-cycle bundle). No regex uses a greedy
`.*`, none has a bare `\ba\b` key word, none shows a heredoc-halved `\b`. FSH and LH appear only in
`notOnThisSpec` lines and in one video warning — correct.

**Note:** all four `note.blocks.json` files were rewritten at 15:55 while this read was in progress (the
`mustknow` callout became an `h` + `p` pair). Block indices below are from the current files. The four
`bundle.json` files are byte-identical to the ones read, re-verified by re-running the full probe.

---

## b2-reproductive-systems

### 1. A figure subtitle contradicts its own block caption — note block [8] (figure, "2. What each male part does")

Current SVG text (second `<text>` element): `The words on the left are the ones the examiner is looking for.`
Current block caption: `The verb on the right is what the mark scheme is reading for.`

The left column is the structure names; the right column is the function. The whole section teaches that
the function is what earns the mark ("Stuck on a function? Give the **verb**"), so the subtitle says the
opposite of the caption two lines below it.

Fix: change the SVG subtitle to `The verb on the right is what the examiner is looking for.`

### 2. The same subtitle is wrong on a question figure — `q.science.b2.b2-reproductive-systems.0009`, part `main`, `figures[0]`

Stem: `Complete the table by giving the function of each part of the female reproductive system.`
Figure prints: `The female reproductive system` / `The words on the left are the ones the examiner is looking for.`
/ `ovary` / `oviduct` / `uterus` / `cervix` / `vagina`, with the right column blank.

The left column is printed for her; the right column is the five marks. The subtitle tells her the column
she does not have to write is the one being marked.

Fix: replace that line with `Write one function in each empty box.`

### 3. A scheme accept is missing from the key words — `q...0001`, part `b`

`scheme[0].accept` = `["transports sperm","takes sperm to the urethra"]`
`answer.keyWords[0].any` = `["carries sperm","carry sperm","transports sperm","carries the sperm"]`

`"it takes sperm to the urethra"` — the scheme's own second accept — scores **0/1**, and so does
`"it takes the sperm from the testis towards the urethra"`.

Fix: add `"takes sperm"` and `"takes the sperm"` to `any`. **Verified:** both phrasings → 1/1;
`"it carries urine"` still 0/1 (and still tags `sci.repro.male-parts-answered-with-urine`),
`"it makes the sperm"` still 0/1.

### 4. Article asymmetry in the key words — `q...0001`, part `c`

`any` contains both `"adds fluid"` and `"adds a fluid"`, but only `"adds liquid"` with no article, so
`"it adds a liquid to the sperm"` scores **0/1**.

Fix: add `"adds a liquid"`. **Verified:** → 1/1.

### 5. The hormone route to the mark cannot be earned in natural wording — `q...0003`, part `a`

Hint: `Two answers earn the mark: what it makes, or the hormone it makes.`
`scheme[0].for`: `makes / releases ova (accept eggs); also makes oestrogen`
`any` has `"makes oestrogen"` and `"produces oestrogen"` only, as contiguous phrases.

`"it makes the hormone oestrogen"`, `"it produces the hormone oestrogen"` and `"it makes the female sex
hormone oestrogen"` all score **0/1**, because the two words are never adjacent.

Fix: add `"hormone oestrogen"` to `any`. **Verified:** all three → 1/1; the swapped-organ answers
`"it carries the ovum to the uterus"` and `"fertilisation happens here"` stay 0/1.

### 6. The passive form of the oviduct's job cannot be earned — `q...0003`, part `b`

`any` = `["carries the ovum", …, "fertilisation takes place","fertilisation happens","fertilisation occurs","site of fertilisation","where fertilisation"]`.
`"the ovum is fertilised in it on its way to the uterus"` and `"the egg is fertilised here"` both score
**0/1** — "fertilised" is not an inflection the engine derives from "fertilisation".

Fix: add `"is fertilised"` and `"are fertilised"`. **Verified:** both → 1/1;
`"it makes ova and oestrogen"` and `"the embryo implants in it"` stay 0/1.

### 7. The commonest way to say "cooler" cannot be earned — `q...0008`, part `c`

`any` = `["cooler","lower temperature","colder","below body temperature"]`.
`"the temperature outside the body is lower, which suits sperm production"` scores **0/1**: the phrase
"lower temperature" never occurs in it.

Fix: add `"temperature is lower"`, `"lower than body temperature"`, `"temperature outside the body is
lower"` and `"cool"` (the last also picks up "cool", "cooling"). **Verified:** all five natural phrasings
→ 1/1 (`the temperature outside the body is lower…`, `…lower than body temperature`, `it is cooler there`,
`it keeps the testes cool`, `because the scrotum is cooler than the body`), while
`"there is more room outside the body"`, `"so the sperm can get out"` and the trap
`"the scrotum is lower down than the rest of the body"` all stay 0/1.

### 8. The worked example's twin scores a wrong answer full marks — `we.science.b2.b2-reproductive-systems.01`, `twin`

Stem: `Give the function of the ovary.`
`answer.keyWords` = `[{"any":["ova","ovum","oestrogen","eggs"],"marks":1}]`

Bare nouns, no verb. **Verified**, each scores 1/1: `"it carries the ova to the uterus"` (that is the
oviduct), `"the ova pass through it"`, `"an ovum implants here"`.

Fix: replace `any` with
`["makes ova","make ova","produces ova","releases an ovum","releases ova","makes eggs","produces eggs","releases an egg","makes oestrogen","produces oestrogen","hormone oestrogen"]`.
**Verified:** all three accepted answers → 1/1, all three wrong answers → 0/1.

### 9. Register: the word "wrong" in learner-facing note prose — blocks [19] and [28]

[19] (examiner callout): `Summer 2024: wrong answers for the **prostate gland** and the testis **centred
on urine**. Naming the structures was not asked for, and a wrong name cost the mark.`
[28] (closing panel): `…leave the name out unless you are sure of it, because a wrong name beside a right
function costs the mark.`

`docs/plan/00-MASTER-PLAN.md:589` — "Feedback never uses the word 'Wrong'". The four already-published B2
notes contain the word zero times; these two are the first.

Fix: `Summer 2024: answers for the prostate gland and the testis centred on urine. Naming the structures
was not asked for, and a name that missed cost the mark.` and `…because a name that misses, written beside
a right function, costs the mark.`

### 10. The video sits under a heading about something else — block [22]

[21] is `h` `5. From a printed diagram to a real length`; [22] is the video `KS3 Biology - Human
Reproductive Systems`; [23] is the paragraph about scale. The video is a second pass over sections 1–4.

Fix: move the video block to sit directly after the section-4 gate (before the section-5 heading).

**Verdict — b2-reproductive-systems:** biology is on-spec and correct throughout (all six male and five
female structures of 2.3.1/2.3.2, fertilisation in the oviduct, FSH/LH only in `notOnThisSpec`); every
number recomputes; the question figures print roman numerals and no names. Ship after the six key-word
additions (3–8), the two figure subtitles (1–2) and the register edit (9).

---

## b2-fertilisation-pregnancy

### 1. A question figure prints the whole of its own three-mark answer — `q.science.b2.b2-fertilisation-pregnancy.0011`, `figures[0]`

Part `a` [3]: `Explain how the placenta is adapted for the diffusion of substances between the mother and the foetus.`
Part `c` [1]: `State the part of the placenta that gives it its large surface area.`

The figure is the unmodified teaching figure from note block [23]. Its `<text>` elements include:

- `A surface for diffusion, the same four points every time:`
- `1  large surface area — the villi fold it`
- `2  thin barrier — a short way to diffuse`
- `3  good blood supply — vessels on both sides`
- `4  the flow keeps the difference in concentration up`
- `villi: the folds that make the surface area large`

All four marks are printed on the page the question is asked on. (The other questions in this bundle do
use stripped figures — 0001/0002 are "no names", 0004 leaves the last row `?` — so the convention exists.)

Fix: publish a question copy of the SVG with the five-line four-point panel deleted and the villus label
shortened to `villi`, and shorten its `<title>` to the question's alt text.

### 2. A question figure prints all three of its answers — `q...0008`, `figures[0]`

Same figure, same problem, three different marks:

| part | stem | text printed on the figure |
| --- | --- | --- |
| `a` [2] | Name two substances that pass from the mother's blood to the foetus across the placenta. | `to the foetus: dissolved nutrients, oxygen` |
| `b` [2] | Name two waste substances that pass from the foetus to the mother's blood across the placenta. | `from the foetus: carbon dioxide, urea` |
| `c` [1] | State the function of the amniotic fluid. | `amniotic fluid cushions the foetus` |

Fix: on the question copy, cut the two arrow labels to `to the foetus` / `from the foetus` and the amnion
label to `amniotic fluid`.

### 3. The six-mark question's figure title gives the answer away — `q...0013`, `figures[0]`

`alt`: `A route from the ovary along the oviduct into the uterus with five numbered stations; only stations
1 and 3 are captioned.`
`<title>` (the figure's accessible name, and the hover tooltip in every browser):
`A horizontal route from the ovary at the left, along the oviduct, into the uterus at the right, with five
numbered stations: ovulation, fertilisation in the oviduct, repeated mitosis into a ball of cells,
implantation in the thickened uterus lining, and differentiation`

The stem is `Describe what happens from the moment a sperm reaches an ovum until the cells of the embryo
begin to differentiate. Use the words zygote, mitosis, implantation and differentiation…`. The on-canvas
captions were correctly stripped; the title was not, so it still names five of the seven indicative points.

Fix: set `<title>` to the `alt` string.

### 4. The key words contradict the pack's own "feature plus consequence" rule — `q...0011`, part `a`

Group 1 demands the consequence (`"large surface area so more"`, `"surface area so more diffusion"`…).
Groups 2 and 3 do not: group 2 accepts bare `"thin barrier"`, `"thin wall"`, `"thin membrane"`; group 3
accepts bare `"good blood supply"`, `"rich blood supply"`.

**Verified:** `"it has a large surface area, a thin barrier and a good blood supply"` scores **2/3**, and so
does the bare list `"large surface area, thin barrier, good blood supply"` — while note block [24] teaches
`Each mark is a feature **plus** what it achieves. A bare "large surface area" earns nothing.` and the
part's own hint says `never a feature on its own`.

Fix — make groups 2 and 3 carry the consequence the way group 1 already does:

- group 2 `any`: `["barrier is thin so","thin barrier so","thin wall so","wall is thin so","distance to diffuse is short","short distance to diffuse","short diffusion distance","thin so the distance","thin membrane so"]`
- group 3 `any`: `["good blood supply so","good blood supply which","good blood supply keeps","rich blood supply so","blood supply on both sides keeps","keeps the difference in concentration","keeps the concentration gradient","maintains the concentration gradient"]`

**Verified:** the bare-features answer → 0/3; the accepted answer, the worked solution and a fresh
paraphrase (`"it has a big surface area so more can diffuse at once, the wall is thin so the distance is
short, and there is a good blood supply which keeps the difference in concentration up"`) → 3/3.

### 5. Higher-tier content taught without the Higher marker — note blocks [24] and [31]

Spec 2.3.3 marks `and explain the role of villi in providing these adaptations` as Higher only
(`textMarked`, bold). The bundle already knows how to say this: block [5] ends the middle-piece line with
`*(Higher Tier.)*`, and `q...0011` and `q...0002` are both `tier: "H"`. But block [24]
(`**Step 2 — large surface area.** The **villi** fold it…`) and the summary line in block [31]
(`Explain how the **placenta** is adapted, as feature-plus-consequence pairs…`) carry no marker, so a
Foundation learner is taught the villi role as if it were hers.

Fix: append `*(Higher Tier.)*` to the villi step in [24] and to that summary line in [31], as block [5] does.

### 6. The video's "why" points at a second video that does not exist — block [27]

`why`: `Cognito is the one of our two videos that reaches the placenta; the Freesciencelessons clip stops
before it. The uploader labels it KS3 because in England the anatomy is taught earlier, but the content is
your B2 content.`

This note contains exactly one video block. The "Freesciencelessons clip" is a leftover from a draft that
had two (the Freesciencelessons contraception clip is in the *b2-infertility-contraception* note), and
"the one of our two videos" is ungrammatical either way.

Fix: `This clip carries on as far as the placenta, which is where most of this topic's marks are. The
uploader labels it KS3 because in England the anatomy is taught earlier, but the content is your B2 content.`

### 7. A one-word key word lets a wrong organ score — `q...0006`, part `b`

Stem: `State exactly where in the uterus the ball of cells implants.`
`keyWords` = `[{"any":["lining"],"marks":1}]`

**Verified:** `"in the lining of the oviduct"` and `"in the lining of the ovary"` each score **1/1**.

Fix: add `"reject": ["oviduct","ovary","fallopian"]` to the group. **Verified:** all four accepted answers
and the worked solution stay 1/1; both wrong answers → 0/1.

### 8. Bare "bigger"/"larger" let a wrong reason score — `q...0012`, part `b`

Stem: `Suggest why the mass gained per week is greater near the end of a pregnancy than near the beginning.`
`any` = `["more cells","many more cells","larger number of cells","bigger","larger"]`

**Verified:** `"the placenta is bigger"` and `"the mother is larger by then"` each score **1/1**.

Fix: replace the two bare words with `"the foetus is bigger"`, `"the foetus is larger"`, `"it is bigger"`,
`"it is larger"`. **Verified:** the worked solution, the accepted answer, `"there are many more cells
dividing at the end"` and the scheme's own accept `"the foetus is bigger, so it grows by more each week"`
all stay 1/1; both wrong answers → 0/1.

### 9. Minor: two different "about" lengths for an oviduct across the set

`q...0010` part `b` opens `An oviduct is about 96 mm long` (chosen so 96 ÷ 1.6 = 60 exactly), while
`b2-reproductive-systems` `q...0004` works one out at 110 mm and its note block [23] repeats `110 mm,
which is 11.0 cm`. Both are inside the real range, so neither is wrong; they just read oddly back to back.

Fix (wording only, keeps the arithmetic): `The oviduct in this dish measures 96 mm.`

**Verdict — b2-fertilisation-pregnancy:** the biology is right and on-spec (haploid nuclei fuse to a
diploid zygote in the oviduct, mitosis to a ball of cells, implantation in the lining, differentiation,
the placenta's four exchange points, cord and amnion) and every calculation recomputes — but two question
figures and one figure title hand the learner the answers, and the three-mark placenta key words
contradict the rule the note teaches. Not ready until 1–4 are done.

---

## b2-sex-hormones-menstrual-cycle

### 1. The find-the-mistake feedback makes an arithmetic claim that is false — `ftm.science.b2.b2-sex-hormones-menstrual-cycle.01`, `feedback`

Current text: `One mark of the two: the rule in line 1 is right, and a scheme that gives a mark for the
method would credit it. A quick sanity check catches the rest — a cycle longer than 28 days must ovulate
later than day 14, and day 19 is later than day 17 by more than the cycle is longer than 28.`

Recomputed: 19 − 17 = **2**, and 31 − 28 = **3**. Two is not more than three, so the sentence offered as
the sanity check is false. The check it is reaching for compares day 19 with day 14, not with day 17:
19 − 14 = 5 against a cycle only 3 days longer than 28.

Fix: `…and day 19 is five days later than day 14 while her cycle is only three days longer than 28.`

### 2. Two QWC indicative points share key words, so one is earned for free — `q.science.b2.b2-sex-hormones-menstrual-cycle.0012`, part `main`

`indicativeContent[0]`: point `menstruation happens on days 1 to 5, when the lining of the uterus breaks
down and is lost`, keyWords `["menstruation","days 1","breaks down","lining"]`
`indicativeContent[6]`: point `if it is not fertilised, progesterone falls, the lining breaks down and the
next cycle begins at day 1`, keyWords `["falls","breaks down","next cycle","day 1"]`

`breaks down` and `day 1` / `days 1` sit in both.

**Verified:** an answer that describes only the first five days — `The cycle starts on day 1, which is the
first day of bleeding. From day 1 to day 5 the lining of the uterus breaks down and is lost. That is
menstruation, and it is the start of the cycle. Nothing else is described here at all, only those first
five days of bleeding.` — shows **2 of 7** points, the second being point 7, which it never says.

Fix: narrow `indicativeContent[6].keyWords` to
`["progesterone falls","level of progesterone falls","no longer maintained","next cycle"]`.

### 3. The scheme accepts two characteristics the key words cannot match — `q...0013`, part `b`

Stem: `Give two secondary sexual characteristics caused by oestrogen.`
`scheme[1].for`: `hips widen (accept body hair grows, or the menstrual cycle begins)`
`keyWords`: group 1 breasts only, group 2 hips only.

**Verified:** `"body hair grows and the menstrual cycle begins"` — two answers the scheme explicitly
accepts — scores **0/2**; `"the breasts develop and body hair grows"` scores 1/2; `"the hips widen and
periods start"` scores 1/2. The table question `q...0006` in the same bundle already accepts `body hair`
and `menstrual cycle begins` for oestrogen, so the bundle contradicts itself.

Fix: give both groups the same full list —
`["breasts develop","breast development","breasts grow","development of breasts","hips widen","wider hips","hips get wider","widening of the hips","body hair grows","body hair","menstrual cycle begins","periods start"]`
(the engine's `used` set stops one phrase collecting both marks). **Verified:** every scheme-accepted pair
→ 2/2 (`breasts develop and hips widen`, `body hair grows and the menstrual cycle begins`, `the breasts
develop and body hair grows`, `the hips widen and periods start`, `breasts grow and the hips get wider`);
one characteristic alone → 1/2; testosterone's characteristics (`a deeper voice and facial hair`,
`the voice gets deeper`) → 0/2.

### 4. Minor: the video sits under a heading about something else — block [20]

[19] is `h` `5. When a cycle is not 28 days long`; the video's `why` opens `Watch it for the puberty and
secondary-characteristics half, which is your content`, which is section 2. Same pattern as the
reproductive-systems bundle. (The FSH/LH half of the `why` is exactly right and should stay.)

### 5. Minor: the graph shows the lining thickening through the half the note calls "maintained"

Note block [5]: `Days 14 to 28: the lining is maintained, ready in case an embryo arrives.`
Strip figure [4]: `maintain` / `progesterone keeps the lining thick`.
The lower curve, read off the SVG: day 14 = 7.00 mm, day 17 = 8.50, day 20 = 10.00, day 24 = 12.00, then
flat to day 28 — a 71% rise inside the "maintained" phase. `q...0011` part `d` marks that rise as the
answer, so the figure and the questions agree with each other; only the word "maintained" jars.

Fix (optional, wording only): `Days 14 to 28: the lining is kept ready — progesterone holds it there while
it finishes thickening.`

**Verdict — b2-sex-hormones-menstrual-cycle:** the strongest of the four. Oestrogen and progesterone only,
FSH/LH confined to `notOnThisSpec` and to an explicit warning on the one video that teaches them; every
graph reading verified against the SVG's own bezier control points (day 5 = 1.00 mm, day 14 = 7.00, day 20
= 10.00, day 22 = 11.00, day 24 = 12.00; solid peak day 13.11, dashed peak day 21.47), so every reading
question and every distractor value is true to the figure; the question graphs are properly anonymised to
"hormone A / hormone B". Fix the false sanity-check sentence (1), the QWC overlap (2) and the puberty key
words (3) and it is ready.

---

## b2-infertility-contraception

### 1. A question figure prints all three of its answers — `q.science.b2.b2-infertility-contraception.0002`, `figures[0]`

The figure is the unmodified teaching figure from note block [4]. One of its `<text>` elements reads:

`the condom (mechanical) — a barrier, so sperm never reach the ovum; it also stops HIV and chlamydia passing between partners`

That single line answers all three parts:

| part | stem | printed |
| --- | --- | --- |
| `a` [1] | Explain how a condom works to prevent pregnancy. | `a barrier, so sperm never reach the ovum` |
| `b` [1] | Give one advantage of using a condom rather than the contraceptive pill. | `it also stops HIV and chlamydia passing between partners` |
| `c` [1] | Name one sexually transmitted infection, caused by a bacterium, that a condom helps to prevent. | `chlamydia` |

Fix: on the question copy of the SVG, delete the four method captions (keep the six chain boxes and the
break symbols) and change `alt` to `A chain of six numbered boxes from an ovum maturing to implantation,
with break symbols on the connectors and no labels on them.`

### 2. The six-mark question's `alt` is false, and the figure prints most of the answer — `q...0008`, `figures[0]`

`alt`: `The same chain of six boxes with three break symbols drawn and no labels on them.`

The figure does carry labels — three of them:

- `the pill and the implant — change hormone levels, so no ovum develops or is released`
- `male sterilisation — the sperm tubes are cut, so sperm cannot leave`
- `female sterilisation — the oviducts are cut, so the ovum and the sperm cannot meet`

The stem is `Describe how each of these three methods prevents pregnancy…`; two of the three mechanisms
(pill, sterilisation) are printed in the mark scheme's own words. Only the condom caption was removed.

Fix: delete the three remaining caption lines from the question copy, which also makes the `alt` true.

### 3. The IVF question's figure prints all four of its marks — `q...0006`, `figures[0]`

| part | stem | printed on the figure |
| --- | --- | --- |
| `a` [1] | State why a woman having in vitro fertilisation is given hormones at the start of the treatment. | `1. hormones` / `given so that several` / `ova ripen at once` |
| `b` [1] | State exactly where the ova are fertilised during in vitro fertilisation. | `3. fertilisation` / `ova and sperm are` / `mixed in a dish in the` / `laboratory`, and the footnote `Higher Tier only. The examiner wants the place named: in a dish in the laboratory, not just 'in a lab'.` |
| `c` [2] | Describe what is done with the fertilised ova at the end of the treatment. | `4. growth` / `each fertilised ovum` / `divides into a ball of` / `cells`; `5. transfer` / `2 embryos are placed` / `into the uterus` |

Fix: on the question copy, keep the five numbered boxes and their step names but drop the descriptive
lines under steps 1, 3, 4 and 5 and the footnote.

### 4. The photo teaches a technique the spec excludes, and contradicts the bundle's own IVF figure — note block [13]

`src`: `/img/science/b2/b2-infertility-contraception-1.png`
`alt`: `A microscope image of a human ovum held steady by a blunt glass pipette on the left while a fine
needle on the right pierces it to deliver a single sperm.`
`caption`: `Fertilisation happening in a dish, under a microscope — the middle step of the treatment in
section 5.`
`prompt`: `Which step of in vitro fertilisation is this, and what is being done here that could never
happen inside the body?`

The source file (verified on Wikimedia: `File:ICSI intracytoplasmic sperm injection.png`, CC0,
Dr Elena Kontogianni) is described there as "Injecting sperm into a human egg" — it is ICSI, not IVF. The
bundle's own figure in block [21] says the middle step is `ova and sperm are mixed in a dish in the
laboratory`, so the caption labels a micro-injection as the mixing step. Spec 2.3.6 names only "in vitro
fertilisation"; this bundle's `notOnThisSpec` already excludes comparable extras ("Embryo screening, egg
freezing and surrogacy") but says nothing about ICSI, and the prompt invites her to think about it.

Fix, either: (a) recaption — `Fertilisation in a dish, under a microscope. In this clinic one sperm is
being injected into the ovum; CCEA asks only that the ova and sperm are mixed in a dish.` — add
`ICSI, injecting a single sperm into the ovum` to `notOnThisSpec`, and change the prompt to
`Which step of in vitro fertilisation is this, and where is it happening?`; or (b) swap the image for one
of ova and sperm together in a dish.

### 5. A common error fires on a learner who gets it right — `q...0001`, part `main`, `commonErrors[0]`

`regex`: `\b(barrier|blocks? the sperm|kills? the sperm|stops? the sperm (getting|reaching)|traps? the sperm)\b`

**Verified:** `"it is not a barrier; it changes hormone levels so no ovum develops"` scores 1/2 and is told
`That is how a condom works. The pill acts inside one body…` — the learner is corrected for saying exactly
what the note taught her to say (block [6]: `Calling it a barrier is the answer for a condom`).

Fix: `^(?![\s\S]*\b(not|never|isnt|is not|rather than) a barrier)[\s\S]*\b(barrier|blocks? the sperm|kills? the sperm|stops? the sperm (getting|reaching)|traps? the sperm)\b`
**Verified:** the denial is no longer tagged (feedback falls back to `1 of 2: still missing stops an ovum
developing`), while `"it is a barrier that stops the sperm reaching the ovum"` → 0/2 and still tags
`sci.contra.pill-mechanism-unknown`.

### 6. The same misfire on the six-mark QWC — `q...0008`, part `main`, `commonErrors[0]`

`regex`: `pill[^.;\n]{0,40}(barrier|blocks? the sperm|stops? the sperm reaching)`

**Verified:** a Band-A answer containing `The pill is not a barrier: it is a chemical method that changes
hormone levels, so no ovum develops or is released.` scores 5/6 and is tagged
`sci.contra.pill-mechanism-unknown`, with the feedback `The pill is not a barrier` — her own sentence read
back to her as a correction.

Fix: `\bpill\b(?![^.;\n]{0,40}(is not|isnt|never) a barrier)[^.;\n]{0,40}(barrier|blocks? the sperm|stops? the sperm reaching)`
**Verified:** the strong answer is no longer tagged; `"The pill is a barrier that stops the sperm reaching
the ovum…"` still tags and still floors at 3/6.

### 7. The IVF absence pattern fires on a correct answer — `q...0006`, part `a`

`keyWords[0].any` = `["several ova","several eggs","more than one ovum","more than one egg","multiple ova","many ova","more ova","more eggs"]`
`commonErrors[0].regex` = `^(?![\s\S]*\b(several|multiple|more than one|many|more (ova|eggs))\b)[\s\S]*$`

**Verified:** `"so that lots of ova ripen at once"` scores **0/1** and is told `The number is the point:
treatment needs several at once, not the one a normal cycle gives` — which is what she wrote. The pattern
also has no positive anchor at all: it matches any string that lacks those words.

Fix: add `"lots of ova"`, `"lots of eggs"`, `"a number of ova"` to `any`, and anchor the pattern on
something the answer must contain:
`^(?![\s\S]*\b(several|multiple|more than one|many|lots|a number of|more (ova|eggs))\b)[\s\S]*\b(ova|ovum|egg|eggs|hormone|hormones)\b`
**Verified:** `"so that lots of ova ripen at once"` → 1/1; `"to make the sperm swim faster"` → 0/1;
an empty answer no longer matches the pattern (`Type an answer first.` instead of the misconception).

### 8. QWC: a contentless answer collects a point, and the model answer misses one of its own — `q...0008`, part `main`

`indicativeContent[6]` = point `a decision that names the couple's own situation`, keyWords
`["because","for this couple","would suit"]`. The stem asks only `Describe how each of these three methods
prevents pregnancy, and give one advantage and one disadvantage of using a condom rather than the pill` —
no decision is requested, and `because` is free in any six-mark answer.

**Verified:** `"I think the couple should choose one because it is what suits them and the doctor would
help them decide what to do next."` shows **1 of 7** points and lands on band C.

`indicativeContent[2]` = point `so no ovum develops or is released`, keyWords `["no ovum","stops
ovulation","ovum develops"]` — none of which matches the pack's own worked answer (`…that stops an ovum
developing so none is released`), so **the model answer shows only 5 of 7 points**.

Fix: add `"ovum developing"`, `"egg developing"`, `"none is released"`, `"no egg"` to point 3; replace
point 7's key words with `["which for","would suit","for this couple","for a couple who"]`.
**Verified:** the worked answer then shows 7 of 7 (band A) and the contentless answer 0 of 7 (band 0).

### 9. The video's framing warns about the grouping but not about the off-spec content — block [10]

`why`: `A quick second pass over the methods, now that you have the chain to hang them on. It is made for
another board, so it sorts them into hormonal and non-hormonal — translate each one back into CCEA's three
groups as you watch, and the question underneath asks you to do exactly that.`

This bundle's own `notOnThisSpec` names two things an AQA-facing contraception clip is very likely to
teach: `Clomifene, diaphragms, intrauterine devices, patches, injections and spermicides` and
`'Inhibits FSH production' as the pill's mechanism (CCEA says 'change hormone levels')`. The
menstrual-cycle bundle's video `why` names FSH and LH out loud; this one does not, even though the pill's
mechanism is the single most examined point in the topic.

Fix (after a human watches the clip to confirm what it actually covers): extend the `why` with
`It also names methods CCEA does not — coils, patches, injections — and gives the pill's mechanism as
"inhibits FSH". CCEA wants "changes hormone levels", so keep that wording.`

**Verdict — b2-infertility-contraception:** the content is right and squarely on 2.3.6/2.3.7 (mechanical /
chemical / surgical, the condom's STI advantage, chlamydia as the bacterial one, hormones for multiple ova,
IVF in a dish, several embryos transferred, Higher-tier material correctly marked and tiered), the survey
and clinic numbers all recompute, and the find-the-mistake is sound. But three question figures print
their own answers, one photo teaches a technique the spec excludes, and three patterns correct learners
for correct sentences. The most work of the four.

---

## Summary

| bundle | blocking | worth fixing | clean |
| --- | --- | --- | --- |
| b2-reproductive-systems | — | 2 figure subtitles, 6 key-word gaps, 1 twin that scores wrong answers, register, video placement | biology, all numbers, all figures answer-free |
| b2-fertilisation-pregnancy | 2 figures print their answers, 1 title leaks the QWC answer | placenta key words vs the pack's own rule, Higher marker on the villi, phantom second video, 2 loose key-word lists | biology, all numbers, photos |
| b2-sex-hormones-menstrual-cycle | — | false sanity-check arithmetic in the FTM, QWC key-word overlap, puberty key words vs the scheme | every graph reading verified against the SVG, FSH/LH handled, all numbers |
| b2-infertility-contraception | 3 figures print their answers, ICSI photo off-spec | 3 patterns fire on correct answers, QWC evidence, video framing | biology, all percentages, FTM, licences |
