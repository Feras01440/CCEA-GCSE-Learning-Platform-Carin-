# Pre-read: C2 batch B — rates, collision theory, equilibrium, energy changes

Bundles read as published at **2026-09-20 16:13:54** (all four were rewritten mid-read; every finding below
was re-checked against that copy). Digests: `docs/dev/qa/pre-read/c2-measuring-rates.digest.txt`,
`…c2-collision-theory-catalysts.digest.txt`, `…c2-equilibrium.digest.txt`, `…c2-energy-changes.digest.txt`.

Method: every number recomputed with node; every text part marked with the real `markAnswer` (accepted answers,
worked solutions, mark-scheme wording, plausible paraphrases, plausible reversed answers); every figure's own text
marked against its question's spec; `plotLattice` and the repo's `lintContent` / `figureLeakWarnings` run directly.
Each proposed fix was applied to an in-memory copy of the spec and re-marked; the before/after marks are quoted.

---

## Affects all four bundles

### A1. The "You can now…" recap is no longer a `mustknow` callout (regression, 16:13 rewrite)

All four notes now end the recap with two plain blocks:

```json
{"type":"h","text":"You can now"}
{"type":"p","md":"use **rate = change ÷ time**, with the unit cm³/s or g/s.\nuse **rate = 1 ÷ time** …"}
```

The 15:40 copy of these same four files, and every other published bundle (`c2-redox` block 33, all of B1), use
`{"type":"callout","kind":"mustknow","title":"You can now…","md":"…use **rate = change ÷ time**, …"}`.
`kind: "mustknow"` is a first-class note block (`src/components/items/gates.ts:25`) that renders as the styled
"Must know · not on the formula sheet" panel (`src/components/items/StepRevealNote.tsx:39`). As rewritten it
renders as an ordinary heading and paragraph, the panel is gone, the heading has lost its ellipsis
("You can now" not "You can now…"), and each line has lost its leading "…", so the lines read as five
lower-case sentence fragments rather than continuations of the heading.

**Fix** — restore the block in all four notes:
`{"type":"callout","kind":"mustknow","title":"You can now…","md":"…use **rate = change ÷ time**, with the unit cm³/s or g/s.\n…use **rate = 1 ÷ time** …"}`, with the leading "…" on every line, exactly as `c2-redox` has it.

### A2. Reaction-profile arrows are double-headed, against the bundle's own instruction

Every profile arrow in `c2-energy-changes` and `c2-collision-theory-catalysts` is drawn with an arrowhead at
both ends, e.g. in `c2-energy-changes` q.0004 / q.0009:

```
<path d="M278 268 V78"/><path d="M274 262 l4 6 l4 -6"/><path d="M274 84 l4 -6 l4 6"/>
```

Counted across both bundles: WE 02 figure 2 arrows / 4 heads, WE 02 twin 2/4, q.0004 and q.0009 2/4,
collision-theory q.0007 and q.0012 3/6, q.0008 2/4, `prompts[11]` 2/4.

`c2-energy-changes` names the opposite as an error in its own misconception list — *"Double-headed arrows used on
a profile instead of single-headed ones (Summer 2024 C2 H Q6)"* — and its worked example says
*"A: a single-headed arrow from the reactant level up to the top of the hump"* and *"Use single-headed arrows:
the report asks for them by name."* The learner is told single-headed and shown double-headed six times.

**Fix** — drop one head from each arrow (keep the head at the far end: for the activation-energy arrow keep
`M274 84 l4 -6 l4 6`, delete `M274 144 l4 6 l4 -6`; for the energy-change arrow keep the head at the product
level). Alternatively soften the worked-example decision line — but the misconception entry cites the report by
name, so the drawings are the side to change.

---

## 1. c2-measuring-rates

### MR-1 (serious) The volume–time curve is drawn flat at every plotted reading

`bundle.questions[0..2].figures[0]`, `questions[8]`, `workedExamples[0].figure` and the note figures
(`note[15]`, `note[20]`) all carry the same path:

```
M72 360 Q122.3 242.7 172.7 242.7 Q223 168 273.3 168 Q323.6 114.7 374 114.7
        Q424.4 82.7 474.7 82.7 Q525 72 575.3 72 Q625.6 72 676 72
```

Every quadratic's control point shares the **y** of its own end point, so the tangent at each 10 s reading is
exactly horizontal and the next segment leaves steeply again. Sampled from the path (axes: x = 72 px at 0 s to
676 px at 60 s, y = 360 px at 0 cm³ to 40 px at 60 cm³):

| t / s | 0 | 2.5 | 5 | 7.5 | 10 | 12.5 | 15 | 17.5 | 20 |
|---|---|---|---|---|---|---|---|---|---|
| drawn V / cm³ | 0 | 9.6 | 16.5 | 20.6 | 22.0 | 28.1 | 32.5 | 35.1 | 36.0 |

Gradient into each knot 1.24 → 0.79 → 0.56 → 0.34 → 0.11 cm³/s, **out of each knot 0.00 cm³/s**, then straight
back up (2.80 cm³/s just after 10 s). The curve reads as five terraces, not as "steep at the start, bending over"
(the figure's own alt). It also contradicts **q.0011**, whose stem states *"Over the first 10 s the curve is a
straight line from the origin, reaching 22 cm³ at 10 s"* and asks for that gradient (2.2 cm³/s): on the drawn
curve the first 10 s is not straight (11.0 would be the straight-line value at 5 s, the drawing gives 16.5) and
its gradient at 10 s is zero.

**Fix** — replace that one `<path d="…">` (leave the grid, axes, labels and markers alone) with a monotone
spline through the seven table points, sampled every second, in the same style as the three-curve figure
`note[26]`, which is already drawn as an `L` polyline:

```
M72 360 L82.1 348.1 L92.1 335.9 L102.2 323.5 L112.3 311 L122.3 298.7 L132.4 286.5 L142.5 274.7 L152.5 263.4
L162.6 252.7 L172.7 242.7 L182.7 233.4 L192.8 224.7 L202.9 216.5 L212.9 208.7 L223 201.3 L233.1 194.3
L243.1 187.5 L253.2 180.9 L263.3 174.4 L273.3 168 L283.4 161.7 L293.5 155.6 L303.5 149.8 L313.6 144.1
L323.7 138.7 L333.7 133.4 L343.8 128.4 L353.9 123.6 L363.9 119 L374 114.7 L384.1 110.5 L394.1 106.6
L404.2 102.8 L414.3 99.3 L424.3 96 L434.4 92.9 L444.5 90 L454.5 87.4 L464.6 84.9 L474.7 82.7 L484.7 80.6
L494.8 78.8 L504.9 77.2 L514.9 75.8 L525 74.7 L535.1 73.7 L545.1 73 L555.2 72.4 L565.3 72.1 L575.3 72
L585.4 72 L595.5 72 L605.5 72 L615.6 72 L625.7 72 L635.7 72 L645.8 72 L655.9 72 L665.9 72 L676 72
```

(Fritsch–Carlson monotone cubic through (0,0), (10,22), (20,36), (30,46), (40,52), (50,54), (60,54); it passes
through all seven markers exactly and gives 11.50 at 5 s, 29.75 at 15 s, 41.50 at 25 s, never turning flat before
50 s. Verified by re-sampling the emitted path.)

### MR-2 (serious) `q.0007` and WE 01's twin refuse the unit their own stem demands

- `q.science.c2.c2-measuring-rates.0007` (main), 3 marks. Stem: *"Calculate the mean rate of reaction in g/s.
  Give your answer to three decimal places."* Spec: `"unit": "grams per second"`.
- `we.science.c2.c2-measuring-rates.01` twin, 1 mark. Stem: *"…in g/s, to three decimal places."*
  Same `"unit": "grams per second"`.

`"g/s"` is not a spelling of `"grams per second"` in `UNIT_ALIASES` (`src/lib/marking/numeric.ts:456`), so the
unknown-unit path compares the strings and fails:

| response | now | after `"unit": "g/s"` |
|---|---|---|
| `0.018` | 3/3 | 3/3 |
| `0.018 g/s` | **0/3** — "The number is right but the unit is not. The answer should be in grams per second" | 3/3 |
| `0.018 g s⁻¹` | **0/3** | 3/3 |

**Fix** — set `"unit": "g/s"` on both. (`"0.018 grams per second"` then stops matching, but no stem asks for
that spelling; both stems say g/s.)

### MR-3 (serious) `q.0004` refuses `s⁻¹`

`q.science.c2.c2-measuring-rates.0004` (main), 2 marks. Stem: *"Use rate = 1 / time to calculate the rate of
reaction. Give your answer in s⁻¹ to three decimal places."* Spec: `"unit": "per second"`.
`0.031 s⁻¹` scores **0/2** ("the unit… should be in per second"); `0.031 s-1` scores 0/2 ("That answer could not
be read"). With `"unit": "s⁻¹"`: `0.031` 2/2, `0.031 s⁻¹` 2/2.

**Fix** — `"unit": "s⁻¹"`.

### MR-4 (moderate) The q.0001 figure's `<title>` states the answer

`questions[0].figures[0]` (also on q.0002) has
`<title id="crg">Volume of carbon dioxide against time: the curve is steepest at the start, bends over and goes
flat at 54 cm³ after 50 seconds</title>`. q.0001 asks *"Use the graph to find the total volume of gas produced"*
— answer `numeric 54 cm³`. A screen-reader user is read the answer before the question. (The repo's
`figureLeakWarnings` checks `<title>` but only against text/mcq/label answers, never numeric, which is why it
passes.)

**Fix** — on the question copy, shorten the title to the shape used by the new q.0009 copy:
*"Volume of carbon dioxide against time: a curve that rises from the origin and then runs level."*

### MR-5 (moderate) The six-mark practical question is shown the apparatus names it must supply

`questions[10]` = `q.…0010`, the 6-mark QWC *"Describe how you would investigate… In your answer name the
apparatus…"*. Its figure prints `gas syringe: read the volume every 10 s` and `conical flask` — two of the
rubric's indicative points. (q.0005 was given a stripped copy in the 16:13 rewrite; q.0010 still has the full one.)

**Fix** — give q.0010 the same stripped copy `questions[4]` now uses.

### MR-6 (minor) Key-word gaps

- `q.…0009/c`, 2 marks. *"the line is steeper and levels off at 54 cm³, just like the first one"* → **1/2**
  ("still missing same volume of gas"). Naming the original plateau value is the strongest form of this answer.
  Fix: add `"54 cm³"`, `"same as before"`, `"like the first one"` to group 1's `any`.
- `q.…0009/d`, 2 marks. *"stand the flask on a top-pan balance and measure how much mass it loses"* → **1/2**.
  Fix: add `"mass it loses"`, `"how much mass"` to group 1's `any`.

### MR-7 (minor) `q.0006`'s table does not enforce its own accuracy instruction

`questions[5]`, stem *"Give each value in grams to two decimal places"*; cells carry
`{"type":"absolute","value":0.005}`, so `["2.9","3.5","3.6"]` scores **3/3**. Cell addressing and entrywise
marking are correct (spec row 2/col 2 → the field's "Row 3, column 3" = 100 s, Loss in mass; a single wrong
cell gives 2/3 with *"Row 5, column 3 should be 3.6, not 3.70"*). Fix, if the instruction is meant to bite:
`{"type":"dp","places":2}`; otherwise drop "to two decimal places" from the stem.

### Clean in this bundle

The `graph` part `q.…0008` is correct throughout: `plotLattice` returns
`x {lo 0, hi 60, major 10, minor 2}`, `y {lo 0, hi 60, major 10, minor 2}`, `unreachable: []` — all seven table
points sit on lattice intersections, and the stated tolerance (absolute 1) is exactly half a small square on
both axes, matching the scheme's *"to within half a small square"*. Every rate recomputed: 54 ÷ 50 = 1.08,
(52 − 36) ÷ 20 = 0.8, 1 ÷ 32 = 0.031, 3.60 ÷ 200 = 0.018, 22 ÷ 10 = 2.2, losses 96.80 − 93.90/93.30/93.20 =
2.90/3.50/3.60. Every numeric common-error value checks out (264, 0.9, 0.93, 2.6, 1.8, 1.25, 0.466, 55.556,
5.28, 60, 1.875, 32) and each earns what its author says. All MCQ items have exactly one correct option, no
duplicate texts, feedback and a misconception id on every distractor. `ftm…01` has exactly one wrong line and
the marks earned match the feedback.

---

## 2. c2-collision-theory-catalysts

### CT-1 (serious) `reject: ["more collisions."]` cancels the part's own key words

The marker drops punctuation before matching, so the reject word is `more collisions`, which is contained in
the very phrases the same group lists as creditworthy.

- `q.science.c2.c2-collision-theory-catalysts.0003` (main), 2 marks. Group 1 `any` includes
  `"more collisions per unit time"`, `reject: ["more collisions."]`.
  *"there are more acid particles in the same volume, so there are more collisions per unit time"* → **1/2**,
  with the feedback *"'More collisions' on its own is the phrasing the scheme refuses"* — though the learner
  wrote the qualified phrase the author listed.
- `we.science.c2.c2-collision-theory-catalysts.01` twin, 3 marks. Group 3 `any` includes
  `"more collisions per unit time"` and `"more collisions each second"`; both are cancelled.
  *"…a larger surface area means more collisions per unit time"* → **2/3**;
  *"…so there are more collisions each second"* → **2/3**, both with *"'more collisions.' cancels the mark it
  sits with."*

**Fix** — delete the `reject` array from that group in both parts. Verified: with it removed,
q.0003 gives 2/2 and 2/2 for the two qualified answers and still **1/2** for the bare
*"…so there are more collisions"*; the twin gives 3/3 and 3/3, and still **2/3** for the bare form. The `any`
list already requires "frequent", "successful" or a time, so nothing is lost.
`q.0004/c` carries the same reject; it has no matching `any` entry, so removing it there too only makes
*"a bigger surface area is exposed, so there are more collisions per unit time"* score 1/2 instead of being
cancelled outright — for consistency with q.0003, add `"more collisions per unit time"` to its group 2 `any`.

### CT-2 (serious) The raising-temperature answer scores on the lowering-temperature question

`q.…0002/b` and `q.…0012/b`, 2 marks each ("Explain your answer in terms of particles", after a **lower**
temperature). Group 2 `any` contains the direction-free `"successful collisions per unit time"`.

*"the particles have more energy, so there are more successful collisions per unit time"* — the exact opposite
answer — scores **1/2** on both parts.

**Fix** — remove `"successful collisions per unit time"` from group 2's `any` (every other entry already
carries "fewer"/"less") and add `"more successful collisions"` and `"more collisions"` to its `reject`.
Verified: the reversed answer then scores 0/2, while
*"the particles have less energy, so fewer collisions have energy at or above the activation energy and there
are fewer successful collisions per unit time"* and *"the particles are moving slower, so there are fewer
successful collisions per second"* both still score 2/2.

### CT-3 (serious) The mark scheme accepts `MnO2`; the marker gives it nothing

`q.…0006` (main) scheme: `P1: manganese(IV) oxide accept ["manganese dioxide","MnO2"]`.
`answer.accepted = ["manganese(IV) oxide","manganese oxide","manganese dioxide"]`, key word `"manganese"`.
`MnO2` → **0/1**, `MnO₂` → **0/1** ("The marking points are not there yet. Expected: manganese.").
Same on `q.…0012/c` (scheme accepts "manganese dioxide"; `MnO2` → 0/1).

**Fix** — add `"MnO2"` and `"MnO₂"` to `accepted` and `"mno2"` to the key-word `any` on both parts.
Verified: `MnO2` 1/1, `MnO₂` 1/1, `manganese(IV) oxide` 1/1, `magnesium oxide` still 0/1.

### CT-4 (serious) The catalytic-pathway answer fails on ordinary synonyms

`q.…0007/b`, 2 marks. Group 1 `any`: alternative reaction pathway / alternative pathway / different pathway /
different route / alternative route. Group 2 `any`: lower / less / reduced activation energy, activation energy
is lowered.

- *"it gives the reaction a different reaction pathway with a smaller activation energy"* → **0/2**
  ("different **reaction** pathway" is not `"different pathway"`; `"smaller activation energy"` is absent here
  though it is listed on q.0012/d).
- *"it provides another route with a lower activation energy"* → **1/2**.
- `q.…0012/d`: *"the dashed route is a different reaction pathway with a smaller activation energy"* → **1/2**.

**Fix** — add `"different reaction pathway"`, `"another route"`, `"another pathway"` to group 1 on both parts,
and `"smaller activation energy"`, `"activation energy is lower"` to group 2 of q.0007/b.
Verified: all three answers then score 2/2, and *"it lowers the energy change of the reaction"* still 0/2.

### CT-5 (moderate) `q.0009/b`: the scheme's own marking-point text scores zero

Scheme `P1: its mass before and after the reaction is the same (2.00 g)`.
Key words `any: ["mass is the same","same mass","mass did not change","mass is unchanged","2.00 both times"]`.

- *"its mass before and after the reaction is the same"* (the scheme's own words) → **0/1**
- *"its mass has not changed"* → **0/1**
- *"it weighs 2.00 g both times"* → **0/1**

**Fix** — add `"mass has not changed"`, `"before and after the reaction is the same"`, `"2.00 g both times"`,
`"weighs the same"`. Verified: all four above score 1/1, and *"it lost mass during the reaction"* stays 0/1.

### CT-6 (moderate) "slows down" earns nothing

`q.…0002/a` and `q.…0012/a`, 1 mark, `any: ["decreases","slower","goes down","reduced"]` (+ `"lower rate"`).
*"the reaction slows down"* → **0/1**; *"the rate slows"* → **0/1**. The stemmer only extends a key word
forwards (`"slower"` never reaches `"slows"`).

**Fix** — add `"slows"`, `"slows down"`, `"goes slower"`. Verified: both score 1/1 and
*"the rate of reaction increases"* stays 0/1.

### CT-7 (moderate) The cube figure prints part (a)'s answer

`questions[3]` = `q.…0004`. (a), 2 marks, asks *"Calculate the total surface area of the 8 small cubes"*
(answer 48 cm²). Its figure prints `surface area 48 cm², volume 8 cm³` under the cut cubes.

**Fix** — give q.0004 a copy with the two "surface area …, volume …" captions removed (keep
`one cube, side 2 cm` and `cut into 8 cubes, side 1 cm`), and keep the annotated copy for the note and the
worked example.

### CT-8 (moderate) The four-box figure states which box the answer is

`questions[10]` = `q.…0011` and `questions[12]` = `q.…0013` use the copy whose footer reads:
*"Only C changes both numbers. That is why a temperature answer needs two ideas and a concentration answer
needs one."* q.0011(a) asks *"which change alters both how often the particles collide and the share of
collisions that succeed?"* and box C is labelled `C  hotter`; the correct option is `raising the temperature`.
q.0013 is the 6-mark QWC whose indicative content is exactly that sentence.

**Fix** — strip those two footer lines from the question copy (keep the `collisions a second` / `successful`
counters, which q.0011 needs to read); keep the full copy for `note[3]`.

### CT-9 (moderate) The catalysed-profile figure prints the explanation it asks for

`questions[6]` = `q.…0007` and `questions[11]` = `q.…0012` use the copy that labels the shorter arrow
`activation energy` + `with a catalyst` and footers *"Both routes start and finish at the same levels: a catalyst
changes the hill, not the ends."* q.0007(b) is *"Explain how the catalyst has this effect"* (marking points:
alternative pathway, **lower activation energy**); q.0012(d)'s named common error is *"lowers the energy
change"*, which the footer refutes in words.

**Fix** — on the question copy keep the two humps and the `(i)`/`(ii)`-style neutral labels but drop the
`activation energy with a catalyst` label and the footer sentence; keep the full copy at `note[31]`.

### CT-10 (moderate) `ftm…03` cannot be repaired by fixing its one flagged line

```
1   The rate of reaction increases, because the catalyst lowers the activation energy.
2 ✘ The total volume of gas collected is larger.
3   This is because the reaction is faster.
correction: ["The total volume of gas collected is unchanged.",
             "This is because the amounts of reactant are the same; the gas simply arrives sooner."]
```

`mistakeLine` is 2, but line 3 is also wrong as written — *"the reaction is faster"* is offered as the reason the
volume is larger, and after line 2 is corrected it no longer follows. The `correction` array rewrites both lines,
so the exercise's contract ("exactly one wrong line; the fix fixes it") is not met, and a learner could
defensibly pick line 3.

**Fix** — make line 3 true either way and correct one line only, e.g.
`3. The reaction is faster than it was without the catalyst.` with
`correction: ["The total volume of gas collected is unchanged: the amounts of reactant are the same, so the gas simply arrives sooner."]`.
(The other three find-the-mistake items in this batch — `measuring-rates.01`, `collision…01`, `collision…02`,
`equilibrium.01`, `energy-changes.01` — each have exactly one wrong line, a one-line fix and earned marks that
match their feedback.)

### CT-11 (moderate) Higher-only content is not marked Higher in the note

CCEA 2.3.4 (temperature, concentration, frequency and energy of collisions, surface area to volume ratio) is
Higher-only, and activation energy is defined at 2.8.3 (Higher). The note marks only
`6. How a catalyst works (Higher)`; sections `1. Not every collision reacts`, `2. More often: concentration and
surface area`, `3. Hotter: more often and harder`, `4. Worked example: writing the temperature answer` and
`7. Faster is not more` carry no marker, and the hero's `can[0]` ("Explain temperature, concentration and surface
area through successful collisions") and `can[2]` ("Draw and read a reaction profile…") are Higher too, on a
topic whose `tier` is `"both"`. The sister bundles do mark theirs (`2. The reaction profile (Higher)`,
`4. Worked example: bond energies (Higher)`, `3. Dynamic equilibrium (Higher)`).

**Fix** — add `(Higher)` to headings 1, 2, 3, 4 and 7, or mark the block that opens the collision-theory run,
and flag `can[0]` / `can[2]` the way the other two bundles do. The questions themselves are tiered correctly
(0001–0004, 0007, 0008, 0011–0013 are `H`; 0005, 0006, 0009, 0010 are `both`, matching 2.3.5 at Foundation).

### CT-12 (minor) `q.0004/b` and WE 03's twin refuse `cm⁻¹`

Both stems say *"in cm⁻¹"*; both specs say `"unit": "per cm"`. `6 cm⁻¹` → **0/2**, `3 cm⁻¹` → **0/1**.
**Fix** — `"unit": "cm⁻¹"` on both (verified: `6`, `6 cm⁻¹` both 2/2; `3`, `3 cm⁻¹` both 1/1).

### CT-13 (minor) Further key-word gaps

- `q.0007/a` (1): *"the hydrogen peroxide decomposes more quickly"* → **0/1**. Add `"more quickly"`,
  `"quicker"`, `"decomposes faster"` (verified 1/1, and *"the reaction is slower"* stays 0/1).
- `q.0001` main (1): *"the lowest amount of energy needed for a reaction to happen"* → **0/1**. Add
  `"lowest amount of energy"`, `"lowest energy needed"`, `"least amount of energy"` (verified 1/1, and
  *"the energy given out by the reaction"* stays 0/1). The same gap is in `c2-energy-changes` q.0003.
- `q.0010` main (2): *"a catalyst only changes the speed of the reaction and the amounts of reactant are
  unchanged"* → **1/2**; group 1 lists `"changes only the speed"` but not `"only changes the speed"`.
- `q.0005` main (2): *"it speeds a reaction up but is used up slowly"* → **0/2**; `"speeds up"` does not reach
  `"speeds a reaction up"`.

### CT-14 (minor) The photo's worked-example copy carries no licence or source link

`workedExamples[1].figure`: `{"kind":"photo","src":"/img/science/c2/c2-collision-theory-catalysts-1.jpg",
"licence":"CC BY-SA 3.0","credit":"W. Oelen, Wikimedia Commons, CC BY-SA 3.0"}`. The note copy (`note[27]`)
adds `licenceUrl` and `sourceUrl`. The image is on disk and `Figure.tsx` does render
`credit (licence)` under it, so the attribution shows; only the link back is missing on this copy.
**Fix** — copy `licenceUrl` and `sourceUrl` onto the worked-example figure.

### Clean in this bundle

Surface-area arithmetic all checks: one small cube 6 × 1² = 6 cm², eight of them 48 cm², volume unchanged at
8 cm³, ratios 24 ÷ 8 = 3 and 48 ÷ 8 = 6 cm⁻¹; the common-error values 6, 24 and 0.167 (= 8 ÷ 48) are each what
their misconception names. Every MCQ and diagnostic item has exactly one correct option, distinct option texts,
real distractors, feedback and a misconception id. The `label` part `q.0008` names its targets `i` and `ii`
only — no leak — with a four-item bank (activation energy, energy change, reactants, products) and target
positions that sit on the drawn arrows (i at [302,110] on the arrow spanning y 78–150 at x = 278; ii at
[470,210] on the arrow spanning y 150–268 at x = 488; `direction` "up"/"down" matches the exothermic profile).
The not-on-spec panel names pressure as a rate factor, Maxwell–Boltzmann curves, collision orientation, enzymes
and other industrial catalysts — the four the specification excludes. No regex is greedy, none fires on a
correct answer, none is backslash-halved; the one negative lookahead
(`\bfewer collisions\b(?![\s\S]*successful)`) does not match either accepted answer.

---

## 3. c2-equilibrium

### EQ-1 (serious) Broken KaTeX in the recap: `$\\ce{<=>}$` (regression, 16:13 rewrite)

`note.blocks.json` block **[31]**, `.md`:

> …say what a **reversible reaction** is, and write it with the `$\\ce{<=>}$` sign.

The JSON holds four backslashes, so the runtime string is `$\\ce{<=>}$`: KaTeX reads `\\` as a line break and
then the literal text `ce{<=>}`, instead of the mhchem equilibrium arrow. It is the only one of the seven `\ce`
segments across these four bundles with the wrong backslash count — the 15:40 copy of this same block had
`$\ce{<=>}$`. (`lintContent` does not catch a *doubled* backslash; `lostBackslashDefect` only looks for lost ones.)

**Fix** — `$\ce{<=>}$` (JSON source `"$\\ce{<=>}$"`).

### EQ-2 (moderate) `q.0007`'s figure still prints marking point 1

`questions[6]` = `q.…0007`, *"A dynamic equilibrium can only be reached in a closed system. Explain why."*
Group 1 `any` includes `"get in or out"`; the (already stripped) figure copy still shows
`closed: nothing gets in or out` and `open: the gas escapes`. Marked mechanically, the figure's own text scores
**1/2** against the part.
**Fix** — replace those two labels with neutral ones (`closed`, `open`, or `stoppered` / `unstoppered`).

### EQ-3 (moderate) `q.0006`'s figure states the definition it asks for

`questions[5]` = `q.…0006`, *"State two things that are true of the reaction at that point"*, marking point 1
*"the rates of the forward and reverse reactions are the same"*. It uses the fully annotated copy, whose footer
reads *"Equilibrium is two rates becoming equal, and a product that has left the container cannot react back."*
**Fix** — give q.0006 the stripped copy `questions[6]` already uses (with EQ-2 applied).

### EQ-4 (moderate) `q.0003`'s figure supplies the word equation

`questions[2]` = `q.…0003`, *"Write the word equation for the reaction between hydrated copper(II) sulfate and
heat, using the sign for a reversible reaction"*; marking point P1 is *"anhydrous copper(II) sulfate and water as
the products"*. Its figure labels the tubes `blue crystals` / `hydrated copper(II) sulfate`, `white powder` /
`anhydrous copper(II) sulfate`, and `heat: the water is driven off` — all three terms.
**Fix** — use the stripped copy the 16:13 rewrite created for q.0002 (`questions[1]`), which keeps
`blue crystals` / `white powder` and drops the chemical names.

### EQ-5 (moderate) `q.0009` uses the annotated rate curves; `q.0008` uses the clean one

`questions[8]` = `q.…0009` (whose part (b) asks for the two equilibrium statements) carries the copy printing
`equilibrium reached at 60 s` and `both rates 4`. `questions[7]` = `q.…0008`, which actually asks her to
describe the curves, correctly uses the unlabelled copy.
**Fix** — swap q.0009 onto the unlabelled copy (`questions[7].figures[0]`).

### EQ-6 (moderate) `q.0001` misses two ordinary phrasings

`q.…0001` main, 2 marks.
- *"the reaction goes in both directions, because the products can turn back into the reactants"* → **1/2**;
  group 2 lists `"products turn back into the reactants"`, which the intervening "can" breaks.
- *"it can go forwards and backwards, and the products turn back into the reactants"* → **1/2**;
  group 1 lists `"goes forwards and backwards"`, which "go forwards" does not match.

**Fix** — add `"go forwards and backwards"`, `"forwards and backwards"`, `"in both directions"` to group 1 and
`"products can turn back into the reactants"`, `"products react to form the reactants"` to group 2.
Verified: both answers then 2/2, and *"the reactants can be returned to their original state"* stays 0/2.

### EQ-7 (moderate) The three "define dynamic equilibrium" parts accept different wordings

`q.0006` main, `q.0009/b` and WE 02's twin ask the same thing with different `any` lists:

| response | q.0006 | q.0009/b | we.02 twin |
|---|---|---|---|
| *"the two rates are equal and the concentrations stay the same"* | 2/2 | **1/2** | **1/2** |
| *"…go at the same rate, and the amounts of reactants and products do not change"* | **1/2** | 2/2 | 2/2 |

`q.0006` lists `"concentrations stay the same"` but its `"amounts do not change"` entry only fires on the bare
form; `q.0009/b` and the twin list `"amounts do not change"` but not `"concentrations stay the same"`.

**Fix** — give all three the union: add `"concentrations stay the same"` and `"concentrations do not change"` to
`q.0009/b` and the twin, and `"amounts of reactants and products do not change"` to `q.0006`.
Verified: every row above then scores 2/2, and *"the amounts of the reactants and the products are equal and the
rates stay constant"* still scores 0/2 on all three.

### EQ-8 (minor) Further key-word gaps

- `q.0002/a` (1): *"anhydrous copper(II) sulphate"* → **0/1**. CCEA accepts the -ph- spelling.
  Fix: add `"anhydrous copper sulphate"` and `"anhydrous copper(II) sulphate"` (verified 1/1, and
  *"copper(II) oxide"* stays 0/1).
- `q.0002/b` (2): *"it goes blue and the test tube heats up"* → **1/2**. Fix: add `"heats up"`, `"gets warm"`,
  `"gets hotter"` (verified 2/2; the reversed *"the blue solid turns white and the tube gets cold"* stays 0/2).
- `q.0009/c` (1): *"the amount of ammonia present at equilibrium would change"* → **0/1**; the list has
  `"amount of ammonia changes"`, which the intervening words break. Fix: add `"amount of ammonia"`,
  `"amount of product"`, `"position of the equilibrium"` (verified 1/1, *"it has no effect"* stays 0/1).

### EQ-9 (minor) "the amount of product formed" against a spec that stops at "direction"

2.4.1 reads *"the direction of a reversible reaction can be changed by altering the reaction conditions"*, and
the bundle's own not-on-spec panel says *"CCEA's Double Award specification stops at the direction can be
changed by altering the conditions: you are never asked to predict which way."* `q.0005`'s stem asks for
conditions *"that could be altered to change the amount of product formed"*, and dx-post item 05 uses the same
wording. It is a small step past "direction" in the direction of Le Chatelier and it is backed by the cited
2025 report, so flagging rather than fixing — but "change the direction of the reaction" would sit exactly on
the specification's words.

### Clean in this bundle

Chemistry is right throughout: blue hydrated → white anhydrous copper(II) sulfate on heating, water driven off,
blue back and heat given out on adding water; `CuSO4.5H2O <=> CuSO4 + 5H2O` and `N2 + 3H2 <=> 2NH3` both
balanced with the reversible sign; the dynamic-equilibrium definition is 2.4.2 word for word (closed system,
rates of forward and reverse reactions equal, amounts of reactants and products constant). `q.0005`'s listing
rule works: *"temperature and pressure"* 2/2, *"pressure"* alone 1/2, *"temperature, pressure and
concentration"* (three when two were asked for) 1/2. The reversed curve answer
*"the forward rate rises and the reverse rate falls until they are equal"* scores 0/3 on `q.0008`, as it should.
All diagnostics and MCQs are single-answer with distinct texts and misconception-tagged feedback; `ftm…01` has
one wrong line, a one-line fix and earned marks that match.

---

## 4. c2-energy-changes

(See **A2** above: every reaction-profile arrow in this bundle is double-headed against the bundle's own
instruction and its own named 2024 misconception. That is the most serious item here.)

### EN-1 (moderate) The six-mark question is shown its whole answer

`questions[9]` = `q.…0010`, the 6-mark QWC *"Explain, in terms of the bonds in the reactants and the products,
why this reaction gives out heat…"*. Its figure prints `bonds broken` / `energy taken in`, `bonds made` /
`energy given out`, *"Energy change = energy taken in to break bonds − energy given out when bonds form."* and
*"More given out than taken in makes the change negative, and the reaction exothermic."* — four of the rubric's
indicative points. (q.0001 and q.0005 were given stripped copies in the 16:13 rewrite; q.0010 was not.)
**Fix** — give q.0010 the `step 1` / `step 2` copy that `questions[4]` now uses.

### EN-2 (moderate) A reversed answer scores full marks on WE 02's twin

`we.science.c2.c2-energy-changes.02` twin, 2 marks: *"In an exothermic reaction, where is the product level
drawn compared with the reactant level, and why?"* Group 1 `any: ["below","lower"]`, `reject: ["above","higher"]`.

*"the reactant level is lower, and energy is released"* — which says the **product** level is the higher one,
i.e. endothermic — scores **2/2** ("Every marking point is there"), because the bare word "lower" is present.

**Fix** — make the key words say what is below what:
`any: ["below the reactants","lower than the reactants","below the reactant level","lower than the reactant level","products are below","products are lower"]`.
Verified: the reversed answer then scores 1/2 (only the energy-direction mark, which is genuinely right), while
*"below the reactants, because energy has been given out"* and *"the product level is drawn lower than the
reactant level because energy is released"* both stay 2/2 and *"above the reactants, because heat is taken in"*
stays 0/2.

### EN-3 (moderate) Three parts refuse ordinary correct wordings

- `q.…0009/a` (1), *"State how the profile shows that the reaction is endothermic"*. The scheme's own marking
  point is *"the products are at a higher energy level than the reactants"* — which scores **0/1**. So do
  *"the product level is above the reactant level"* and *"the products end up at a higher energy level than the
  reactants"*. Only the exact `"products are above the reactants"` works.
  **Fix** — add `"product level is above"`, `"products are at a higher energy level"`,
  `"higher energy level than the reactants"`, `"above the reactant level"`. Verified: all four score 1/1, and
  *"the products are below the reactants"* stays 0/1.
- `q.…0002` main (1), *"State what is meant by an endothermic reaction"*. *"one that takes in energy from its
  surroundings"* → **0/1**; *"the temperature falls because heat is absorbed"* → **0/1**. The list holds
  `"energy is taken in"` and `"absorbs heat"` but not these orders.
  **Fix** — add `"takes in energy"`, `"energy taken in"`, `"heat is absorbed"`, `"absorbed"`. Verified: both
  score 1/1, *"a reaction that gives out heat"* stays 0/1.
- `q.…0005` main (2), *"State what happens to energy when bonds are broken and when bonds are made"*.
  *"you have to put energy in to break bonds, and energy is released when bonds form"* → **0/2**;
  *"energy is needed to break a bond and energy is released when a bond is made"* → **0/2**. Both are complete,
  correct GCSE answers.
  **Fix** — add `"energy to break"`, `"energy is needed to break"`, `"energy in to break"` to group 1 and
  `"energy is released when"`, `"released when bonds are made"`, `"released when a bond is made"`,
  `"energy out when bonds are made"` to group 2. Verified: both score 2/2, and the swap
  *"breaking bonds gives energy out and making bonds takes energy in"* stays 0/2.
- `q.…0003` main (1): the same `"lowest amount of energy"` gap as collision-theory CT-13.

### EN-4 (minor) A contradictory answer keeps the reason mark

`q.…0001/b` (2), *"Name the type of reaction, and give the reason"*.
*"endothermic, because the temperature rose"* scores **1/2** — group 1 rejects "endothermic", group 2 credits
"temperature rose". Defensible as independent marking points, but a CCEA marker would not credit a reason that
contradicts the named type. If it should score 0, add `"endothermic"` to group 2's `reject`.

### Clean in this bundle

Every bond-energy figure recomputes: H–H 436 + Cl–Cl 242 = 678 kJ broken, 2 × H–Cl 431 = 862 kJ made,
678 − 862 = **−184 kJ**, exothermic; reverse 862 − 678 = **+184 kJ**, endothermic; methane
4 × 413 + 2 × 498 = 2648 broken, 2 × 805 + 4 × 464 = 3466 made, **−818 kJ**. The sign convention (bonds broken
− bonds made; negative = exothermic = products lower) is used consistently in the worked example, the
questions, the diagnostics, the profiles and the find-the-mistake. Every common-error value is what its
misconception names: 184 (made − broken), 1540 (678 + 862), 247 (one H–Cl counted), 1356 (reactant bonds
doubled), 431 (product bond counted once), −358 (each kind of bond counted once), 818 (methane, sign reversed),
−6.5 (change subtracted backwards), −184 (forward answer copied to the reverse). The four bond energies are the
standard data-book values. All diagnostics and MCQs are single-answer, distinct, with feedback and a
misconception id. The `label` parts `q.0004` and `q.0009/b` name their targets `i` and `ii` only, with a
four-item bank and positions that sit on the drawn arrows of the endothermic profile ((i) [302,200] on the arrow
spanning y 78–268 at x = 278, (ii) [470,210] on the arrow spanning y 150–268 at x = 488, both `direction: "up"`,
correct for endothermic). `ftm…01` has exactly one wrong line, a one-line fix and earned marks matching its
feedback. The not-on-spec panel names enthalpy notation, calorimetry, mean bond enthalpies and the data-leaflet
bond energies.

---

## Clean across the batch

- **Register and mechanics.** No unpaired `$`, no `${` placeholder, no `undefined`/`NaN`, no unbalanced
  `\ce{…}`, no exclamation mark in any learner-facing string (the six `!` in the JSON are all inside regex
  negative lookaheads), and "wrong" never appears as a verdict — only idiomatically ("the wrong way round",
  "what went wrong"). Every note opens with a `hero` carrying a lede, exactly three `can` items and a
  `minutes` value (12, 13, 10, 10); every section between gates is 60 words or fewer, well under 150; every
  closing "In the exam" panel is a pointer of 55–70 words.
- **Regexes.** 38 text common-error patterns across the four bundles: none uses `.*`, none fires on its own
  part's accepted answers, worked solution or scheme text, none matches a bare `\ba\b`, and none shows the
  halved-backslash signature.
- **Multiple choice and diagnostics.** 8 pre-check items, 24 post-check items, 3 MCQ question parts and every
  worked-example `whyMenu`: exactly one correct option each, no duplicate option texts, every distractor
  genuinely wrong, every option with feedback, every distractor with a misconception id.
- **Quality of written communication.** All four 6-mark parts: the worked answer shows 7/7 indicative points,
  word counts 96–113 against minimums of 70–80, band A, and a contentless answer shows 0 points.
- **The repo's own checks.** `lintContent`, `lintNoteBlocks` and `figureLeakWarnings` all return zero for all
  four bundles; `plotLattice` reports no unreachable target. The leaks and the doubled backslash above are
  outside what those three currently look at.

---

## Verdicts

- **c2-measuring-rates** — hold: the rate curve is drawn flat at every reading (MR-1) and two rate questions
  reject the unit their own stem demands (MR-2, MR-3); everything else is small.
- **c2-collision-theory-catalysts** — hold: a reject word cancels the part's own key phrases (CT-1), the
  raising-temperature answer scores on the lowering-temperature question (CT-2), `MnO2` scores nothing though
  the scheme accepts it (CT-3), and the pathway answer fails on ordinary synonyms (CT-4).
- **c2-equilibrium** — hold: `$\\ce{<=>}$` in the recap is broken KaTeX (EQ-1, introduced at 16:13); the rest
  are figure leaks and key-word gaps that are quick to close.
- **c2-energy-changes** — hold: every profile arrow is double-headed against the bundle's own instruction (A2),
  a reversed answer scores 2/2 on WE 02's twin (EN-2), and three parts refuse the mark scheme's own wording
  (EN-3). The chemistry and every number are sound.
